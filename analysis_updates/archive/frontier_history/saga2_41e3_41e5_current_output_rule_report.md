# saga2 41e3-41e5 current output rule

## Current safest answer

when asked what `41E3-41E5` currently supports, the safest output is:

**strongest carrier, not yet a numeric-side collapse point.**

## Why

because the band already strongly supports:

- retained refinement handoff
- `0E/0F` code-family split carry

but still does not force:

- exact raw `0|1` side naming
- direct numeric-side collapse

## Upgrade rule

only upgrade the output if all of these become supportable together:

- side split preserve
- raw numeric co-carry
- no hidden remap before `41E7-41E9`

## Until then

keep:

- deferred numeric binding
- same-side correspondence wording
- no numeric-led semantic branching
