# RNG Module

## 対象

TypeScript `rng` モジュールで再現する領域。

- 戦闘ダメージや分岐に使う乱数供給
- 将来的な成長 / ドロップ / 変身補助乱数

## 現時点の直接ソース

- `reports/saga2_damage_formula_pass21_report.md`
- `reports/Saga2DamageModelPass21.gd`
- `reports/Saga2DamagePipelinePass22.gd`
- `reports/saga2_growth_rng_structural_report.md`
- `reports/saga2_growth_rng_structures.json`
- `reports/saga2_random_seeds_callers_report.md`
- `reports/saga2_rng_entrypoints_report.md`
- `reports/saga2_rng_wait_io_report.md`
- `reports/saga2_ff89_behavior_report.md`
- `reports/saga2_0469_state_machine_report.md`
- `reports/saga2_input_wrappers_report.md`
- `reports/saga2_rng_battle_reachability_report.md`
- `reports/saga2_043e_contract_report.md`
- `reports/saga2_0306_divmod_report.md`
- `reports/saga2_02f0_mul_report.md`
- `reports/saga2_043e_callsite_report.md`
- `reports/saga2_rst10_report.md`
- `reports/saga2_rst18_report.md`
- `reports/saga2_rng_slot_classification_report.md`
- `reports/saga2_rng_battle_slot33_report.md`
- `reports/saga2_particle_state_machine_report.md`
- `reports/saga2_rng_slot07_08_offset_report.md`
- `reports/saga2_rng_damage_core_gap_report.md`
- `reports/saga2_normal_attack_entry_gap_report.md`
- `reports/saga2_battle_runtime_entry_report.md`
- `reports/saga2_battle_state_helpers_report.md`

## ROM から直接確定した構造

- `data_rng` は `bank $0F:$4000` の 256 byte table
- 次ラベル `data_sine` が `0x4100` にあるため、サイズはちょうど 256 byte
- テーブルは `0x00-0xFF` を一度ずつ含む順列になっている
- `00:0440` 付近に `random_seeds ($C0A0)` を increment して `0F:40xx` を引く強い参照候補がある
- `00:0258` 付近に `random_seeds ($C0A0)` を 64 byte 初期化する強い参照候補がある
- `00:0258` は起動シーケンスから fallthrough で到達する
- `00:0469` は `00:0493` / `00:0CC8` などの wrapper が存在する入力 repeat 制御候補
- `00:068F` は RNG 専用ではなく、入力待ち / フレーム更新の共有ルーチン候補
- `FF8A` は wrapper の戻り値に使われる有効入力値候補
- `FF89` は `00:0469` の分岐条件に使われる入力/状態フラグ候補
- `FF89` は `00:29A3` で 0..4 の方向 index 風値へ変換されるため、direction/button bitfield の可能性が高い
- `C774=0x1E/0x05` は key repeat の初回遅延 / 連続間隔候補
- `00:049D` / `00:04A6` は `FF89==0` を待つ input release wrapper 候補
- `00:043E` は dispatch table 公開入口候補で、`0440` はその内部本体の可能性が高い
- `00:043E` は `A=seed slot` と `D/E=range` を受け、`data_rng` 生値を modulo で範囲縮小して `A` で返す helper 候補
- callsite では `DE=$FF00/$0300/$0F00` が現れ、`E=lower`, `D=upper` の向きが強く支持される
- `00:0306` は `H/L -> quotient,remainder` の div/mod helper 候補で、`043E` は remainder 側を使っている可能性が高い
- `00:02F0` は `H*L -> HL` の 8bit multiply helper 候補
- `RST $10` は `06B0` 実行後に VBlank 近辺まで待つ frame sync helper 候補
- `RST $18` は `A` を DMA source page として OAM DMA を起動する helper 候補
- `00:068F` は `RST $18` を使って OAM staging page を OAM へ反映している可能性が高い
- slot `07/08` は battle / item 系 caller、`0C/0D` は 0..1 binary pair caller、`00/01/09/0A/20` は action / field / script 側 caller で見えている
- bank `0D:4440` の slot `07/08` は上位/下位 byte を別々に引いて 16bit offset を作り、`HL` から差し引く pair RNG の可能性が高い
- bank `0D:5741` では slot `33` が `DE=$1300` とともに使われ、battle 側で `0..19` index を生成している可能性が高い
- `56C0` クラスタは `window_buffer_1.particle_*` を使う particle system で、slot `33` は particle 初期配置 RNG の可能性が高い
- battle runtime helper `0D:435A` は actor page 群の同 offset byte 合計 helper 候補
- battle runtime helper `0D:4361` は bank `0C:4680 + index*2` の 2 byte table を引く state/descriptor dispatch helper 候補
- `0D:4178` 以降の `actors ($D803)` は `0xFF` sentinel の 2 byte queue entry 群候補で、action resolve 前処理に見える
- `01E3` は `JP $18CE -> CALL $04BF ; 03 5D 0F ; RET` の thin wrapper で、battle queue 中の固定 dispatch hook 候補
- `437E` は `D0xx..D4xx` actor page を `C206` ベース work buffer へ正規化展開する state staging helper 候補
- `4579` は `D040` 付近の battle stat bits を走査して `actors ($D803)` へ 2 byte entry を積む queue builder 候補
- `0D:43FB-443A` は 9 個の 16bit entry を走査し、`0C:6F82 + entry*8` から 2 byte の集約 flag を作る prepass helper 候補
- より具体的には `43FB-443A` は `D?2D/D?2E` を 0 初期化し、9 個の 2byte entry から `0D:6F82 + entry*8` の 8byte record を引き、先頭 byte の `0x30` class bits に応じて record 4 byte 目を `D?2D` または `D?2E` へ OR fold する helper と読むのが自然
- `40BC: CALL $43FA` の caller から見ると、この 9件 list の実体は `BC` ではなく `HL` 入力で、`HL=$D012` から始めて `INC H` しながら 8 page (`D012..D712`) に対して回している可能性が高い
- さらに `4090-40B4` では `C2DA` の 14 個の 2byte entry を走査し、high nibble で選んだ `D?` page の `+2A/+2B/+2C` に `sourceIndex / 01 / payload` を書く seed/setup loop が見えている
- `C2DA` の producer 候補としては `01:634A-6365` の low-nibble 条件 writer と `01:5B70-5B91` の packed-entry builder が有力で、battle prepass 用の 14件 tableは複数 helperで段階的に組まれている可能性が高い
- `01:5B95` と `01:60E2` はどちらも実質 `C21F + 16 * index` を返す同型 helper とみられ、`C2DA` high nibble は `C21F` 側 16byte block selector としても使われている可能性が高い
- `01:60C0-60E1` は 2byte entry 列を 14 件走査し、low nibble 非 0 entry について `C21F + 16*block` の offset `+0` に `sourceIndex C` を書く block builder と読むのが自然
- `0D:443B-4499` は battle work record から base pointer / span を取り、slot `07/08` で 16bit signed offset を作って pointer record へ書き戻す battle/RNG bridge 候補
- `449A-44F3` は record expansion helper 風だが、`443B-4499` の continuation とまではまだ確定していない
- `4024-4075` 実バイトから、`44F4` caller は `D5xx` 3-page source loop、`443A` caller は `D500/D600/D700` 3-page prepass loop、さらに中間に `D0xx` 5-page staging loopがあると読める
- `1918` は `JP $1B99` wrapper で、`1B99` は `party_order ($C2A0)` の 2-bit field から `HL += 0x20 * slot` を作る address helper 候補
- `44F4-4575` は `D?xx` page の low offsets と `0F:6EC0` / `0D:6F80` table を使って `DE00` scratch record を組む deterministic staging helper 候補
- `44F4` が作る `DE10` は直後に `D849/D949/DA49` family へ書き戻されるため、`DE00` は battle controller へ戻る staging record の可能性が高い
- `4024-403E` で `DE10 -> D849/D94A/D94B` 相当の metadata writeback を行い、`4048-405A` では `D0xx` 5-page loop で `1918` と `449A` を呼び、その後 `4067-4075` で `D500/D600/D700` に対して `443A` を回す
- `449A-44EE` は `HL` source から `D?00-20` / `D?40` 付近の page-local battle work layout へ fixed record を展開する deterministic expander 候補
- `common.i` と照合すると `449A` / `437E` / `actors` loop は `D?40=status`, `D?43=item_id`, `D?45 target近辺`, `D?12..=inventory起点` でかなり整合し、`D0xx` page を戦闘用 working set とみるのが自然
- `D?43/44/45/46` は `00D2 -> C1A5-C1AC` small descriptor とかなり自然に重なり、`item_id / param / target / slot_index` 系の compact action head とみるのが現時点で最も安全
- ただし `42D4-442A` 全域をそのまま `C1A5-C1AC` build とみなすのは粗く、`42B9-4309` は `C200` / transformation 側、`4417+` は action class 判定側として分けて追うほうが安全
- さらに `4417` という番地は `43FB-443A` の内部 `CALL $00D2` 着地点にも現れるため、prepass helper 内部の `4417` と battle core の `443B+` / class 判定帯を混同しないほうが安全
- そのため以前の「`BC=$D500` が `443A` caller page family」という整理は弱く、`43FA-443A` の主入力はむしろ `D?12..` slot list として扱うほうが安全
- `43FA` の直前には `D?2A/2B/2C` を埋める deterministic prepare step もあるため、battle.data page の prepass は `seed pages -> fold slot classes` の 2段で見るほうが安全
- `C2DA` の各 entry は現時点では `high nibble = page selector`, `low nibble = kind/slot id`, `byte1 = payload` の packed seed entry と仮置きするのが最も整合する
- `C21F` は battle prepass 中間の 16byte-block table base 候補で、少なくとも block offset `+0` に source index / owner index 類を持つ可能性が高い
- `60B8` から `60C0` へ fallthrough する直前で `HL=$C2DA`, `DE=$C7E0`, `BC=$0E00` が積まれているため、`60C0-60E1` の flat sentinel list は `C7E0..C7ED` の 14 byte とみてよい
- ただし `01:5B64-5B90` に `C7E0` を `FF` 埋めしたうえで `C2DA` high nibble 0 entry の source index を flat に詰めるほぼ同型 builder があるため、`C7E0` を battle 専用 scratch とみなすのは危険
- bank0 `1237-124B` と `10B2-10C2` でも `HL=$C7E0 + index ; LD A,(HL) ; CP $FF` の read path があり、`C7E0..C7ED` は item / magi / selection setup をまたぐ共有 14-byte candidate list とみるほうが安全
- したがって `C7E0` は `common.i` 上の `script_arg_magi` という名前より広く、shared selection workspace / sparse candidate list として扱うほうが移植上安全
- `1237-124B` (`script opcode 0x1D`) と `109F-10C2` (`script opcode 0x1C`) はどちらも low-range selector を `C7E0 + index` へ通しており、`LD A,(HL); CP $FF` の形から `C7E0` 各 entry は候補IDより `resolved source slot / physical slot index` を返す sparse remap table とみるほうが自然
- したがって現時点では `C7E0[logicalIndex] = physicalSlotIndex | 0xFF` という shared selector workspace 仮説が最も整合する
- `10CC` は単独 helper というより high-range selector dispatcher 本体で、`C71D`, `C2B9`, player inventory-derived pointer, `D906` pointer table など複数経路を `HL -> 1byte candidate ; A=(HL); CP $FF` という共通契約へ正規化している可能性が高い
- このため `C7E0` low-range path も `10CC` high-range path と同じく、「最終 1byte candidate value を返す selector 経路のひとつ」とみるのが安全
- `1551/1552/1554` は `10CC` の resolver 本体ではなく、selector で得た 1byte/16bit 値を bank `0F` の `8byte` record table (`6640` / `6EC0`) に通して `C785` 表示バッファへ展開する terminal helper 群とみるのが自然
- より具体的には `1551` は `(HL)` の 1byte index を、`1552` は `L` の 1byte index を、`1554` は `HL` の index 値をそのまま使い、共通本体で `DE + index*8` を bank `0F` から引いて `15B1` で `C785` へ 0-terminated buffer 化している可能性が高い
- `15B1` は trailing `FF` を落としつつ `C785` を組む text buffer helper、`15CE-15F1` は別系統の numeric formatter とみるほうが自然で、selector 解決と presentation terminal を分けて扱うのが移植上安全
- `1237` 後段では low-range / high-range selector の返り値 1byte を `ADD A,A ; HL=$C2DA ; RST $00` で `C2DA[index*2]` へ通しているため、`C71D` / `C2B9` / `C7E0` はいずれも `C2DA` 14件 table の entry index として使える shared source index 空間を返している可能性が高い
- 同じ返り値は `109F/10D4` 側では `6640` / `6EC0` の 8byte name record table index としてもそのまま使われており、`C2DA` packed entry と name table が同一 source index 空間上で対応づいている可能性が高い
- `1237` では selector 返り値 index で引いた `C2DA[index*2]` の low nibble を `15CE` numeric formatter へ渡しているため、shared source index は単なる name ID ではなく、`C2DA` packed entry と UI 表示の両方へ接続する key とみるのがより自然
- `6640` / `6EC0` 自体は `FF` padding を含む `8byte` fixed-length record table とかなり強く読め、少なくとも script/item selector 系では `resolved source index -> text record` の presentation lookup を担っている可能性が高い
- caller 分類で見ると、`6EC0` は `104B-10C9` cluster の inventory / script-arg / dynamic slot 解決寄り文脈に集中し、`6640` は `10D4-1169` cluster の high-range selector / fixed table / entity-class 解決寄り文脈に集中している
- したがって移植時は `ResolvedSourceIndex` 自体は共通化しつつ、presentation lookup は `6EC0` 側と `6640` 側で分けて持つほうが安全
- ただし `C71D` / `C2B9` 自体は ROM static table より WRAM runtime selector buffers とみるほうが自然になってきた。`C71D` には direct write (`3F08`) があり、`6493-64A8` では 8 件 * 2 byte の pair table が明示的に構築され、`C2B9` には clear / scan / read helper がそろっている
- したがって以前の「`C71D/C2B9` は fixed selector table」という整理は弱く、`10CC` high-range 側も ROM 定数 selector というより事前構築済み WRAM selector buffers を読む reader として扱うのが安全
- さらに `C71D` builder は少なくとも `3F08-3F1C` の single-entry writer 系と `6493-64A8` の 8-entry bulk builder 系に分かれて見えるため、同じ backing buffer を複数 subsystem が上書き再利用している可能性が高い
- どちらも pair byte0 を主たる `sourceIndex` として持ち、byte1 は `0C:7E80 + sourceIndex` 由来の補助 byte 候補なので、移植時は `C71D` storage と builder API を分けて持つほうが安全
- `C2B9` も `6797-679C` の全体 clear、`663F-6646` の単一 entry tombstone、`6657-6668` の active scan、`5D84/65CA/1155` の reader がそろっており、fixed selector table より 16 件 * 2 byte の mutable workspace と見るほうが自然
- したがって selector-runtime は少なくとも `presentation-oriented C71D` と `stateful candidate workspace C2B9` を分けて持つ設計が安全
- `65C9-65E0` は `C2B9[index]` を `C73B/C73C/C760` へ正規化する decode bridge とみるのが自然で、`byte0` は `FF` sentinel を持つ primary `sourceIndex`、`byte1` は `0C:7E80 + sourceIndex` 由来 byte と比較される aux/class byte 候補として読むのが最も整合する
- さらに `6669` は `sourceIndex` を `0F:7860 + index*2` 系の `3byte` descriptor に展開して `C760` へコピーしている可能性が高く、`C2B9` から presentation/usage 側へは直接ではなく decode scratch を一段挟む設計が安全
- `0F:7860` の先頭 dump では 3byte record がすべて `xx yy 00` になっており、`6669` が `C760` へコピーしているのは opaque descriptor というより `little-endian 16bit value + 00 pad` とみるほうが自然
- したがって `C2B9` decode bridge は、`sourceIndex -> value16 lookup (7860)` と `classByte` 比較/補正を束ねる段として持つのが実装寄り
- `65F8-6618` の差異補正部では `0183 -> 040B` が `C760` 3byte scratch の `24bit / 8bit` 除算 helper、`0180 -> 03DC` が `24-step shift/add` の乗算/拡張 helper と読むのが最も整合する
- mismatch 時の順序は `divideBy(canonicalByte from 0C:7E80[sourceIndex]) -> multiplyBy(entry byte C73C) -> divideBy(2)` に見えるため、`C2B9.byte1` は単なる aux byte ではなく `value16/24` を再スケールする class/rank byte 候補へ上がった
- `6610-6618` では補正後の `C760` low 3byte がすべて 0 のとき byte0 を `01` へ clamp しているため、この scratch は opaque descriptor より quantity/value 系の数量 scratch とみるほうが自然
- `6632: DE=$C2A2, HL=$C760, CALL $0162` は `0162 -> 0390` の 3byte add helper を通して `C2A2 += C760` を行う immediate consumer 候補で、`C760` は採用された candidate の value scratch とみるのが最も整合する
- その直後 `663B-6647` では `C70D` current index で引いた `C2B9[index]` を `FF,FF` tombstone 化しており、selector-runtime は `decode -> rescale -> clamp -> accumulate -> tombstone` の 1サイクルで current candidate を消費している可能性が高い
- `0165 -> 03A6` は `[DE] -= [HL]` の 3byte subtract helper、`0168 -> 03BC` は carry を `dst < src` に使う 3byte compare helperとみるのが自然で、`C2A2` を扱う helper family は `add / subtract / compare` まで一通り揃っている
- 実際に `62D1: DE=$C2A2, HL=$C745, CALL $0168 ; JR C,... ; CALL $0165` と `6554: DE=$C2A2, HL=$C70A, CALL $0168 ; JR C,... ; CALL $0165` が見えているため、`C2A2` は単なる accumulator ではなく後段で candidate value と比較・減算される 3byte budget/pool とみるほうが整合する
- `6548-6554` では `DE=$C70A` に `CALL $6669` を当てた直後に `HL=$C70A` で compare/subtract しているため、`C70A` は current sourceIndex から都度作る単発 3byte cost/value scratch とみるのが自然
- いっぽう `64B0-64BC` では `HL=$C71D`, `DE=$C745`, `B=$08` で `CALL $6669` を 8 回回しており、`6669` が `DE` を 3 byte 進めることを踏まえると `C745` は単一値より 3byte cost table base とみるほうが整合する
- ただし `6310-632D` では `0156 -> 0376` の 16bit subtract helperで得た delta を `C745/C746` に `ADD HL,DE` して書き戻しているため、この局面の `C745/C746` は少なくとも pointer/head workspace として使われている
- したがって `C745` は単一の固定 struct より、phase に応じて `cost head / table base / pointer head` を再利用する selector workspace として扱うほうが安全
- `62A2-6330` 全体を見ると、cluster は `C745` head clear -> `630B` による record scan/head update -> candidate gate -> `C2A2` vs `C745` spend判定 -> success/fail side effect -> `0174/01A7` advance loop、という高位 selector-budget flow にかなり自然にまとまる
- このため `C2A2` / `C745` / `C70A` / `C71D` は単独変数としてより、phase-based selector cluster state としてまとめて持つほうが移植上安全
- `62DD: CALL $6332` の success-side helper は、`C709` を保存しつつ `0..3` player loop を回し、`C206/C204/C20F/C785` を巻き込む `636C` worker と、`C2DA` 14件に対する `0C:7F80` 再正規化 pass を持つ post-success rebuild helper 候補とみるのが自然
- したがって success path は単なる演出ではなく、budget spend 後に selector/runtime tables を再構築する段を含んでいる可能性が高い
- `019B -> 05D9` は `FF8B` mode で stride を切り替える index helper とみるのが自然で、通常経路では `party_order ($C2A0)` 経由の `base + 16*index`、別経路では `base + 0x100*index` を作っている可能性が高い
- したがって `6332` の `C206/C204/C20F` は少なくともこの文脈では 16byte stride の player-local record 群として読むのが整合しやすい
- `0198 -> 0608` は `E=0` 固定で `063E -> 0661 -> C2F6` 系 table を見る predicate helper 候補で、caller では `AND A` / `JR Z` による真偽判定として使われている
- このため `6332` の `633F: CALL $0198 ; JR Z,$6347 ; CALL $636C` は、「通常の 4 回 loop に加えて、predicate が真のときだけ追加でもう 1 件 rebuild する」と読むのが安全
- `405C` 側でも `CALL $0198` の結果で `D400/D401` clear を入れるかどうかを切り替えているため、`0198` の subsystem 共通意味は heavy check より `optional page/slot/entry presence predicate` とみるほうが整合する
- したがって移植時は `checkOptionalEntryPresence()` くらいの抽象 API として battle/selector の両側から共有するのが安全
- いっぽう `C2F6` 自体は現時点で direct writer / direct reader がほぼ見えず、`0198` から point read される shared state / shadow table に見えるため、producer は point write より bulk init / block copy 側を疑うほうが安全
- bank0 helper 群では `0080` が `HL -> DE` の `B` byte copy、`0089` が `HL -> DE` の `BC` byte copy、`006D` が zero-fill 本体とかなり自然に読めるため、`C2F6` producer 探索は direct store から `C2E0-C2FF` をまたぐ bulk copy/fill caller 探索へ切り替えるのが筋がよい
- 現時点で `0080/0089` caller の `C2xx` 近傍 high-confidence cluster は主に `C200/C20F` 側に偏っており、`C2F6` を直接含む base/copy はまだ見えていないため、`C2F6` は visible player-local record 直系より別レイヤの shared optional-entry state とみるほうが整合する
- `C2xx` を destination にする bulk copy caller を見ても、高確度なのは `A600 <-> C200` の large copy cluster で、`C2F6` を含む `C2E0-C2FF` base copy は依然として未検出だったため、`C2F6` は visible/local record export-import とも別レイヤの hidden shadow state である可能性がさらに上がった
- `6748-6803` 周辺は `A600 <-> C200` の large block import/export cluster とみるのが自然で、`01C8/01CB/000B` もこの transport cluster の前後処理として読むほうが整合するため、`C2F6` producer 探索からは外してよい
- `6748-6803` 周辺の wrapper 対応は `01C8 -> 04F4`, `01CB -> 04FB` が正しく、以前の `091A` 混入は transport cluster 境界の取り違えだった
- 高確度に読める `006D` caller は `C380` clear, `C7E0..C7ED` fill, `C20F + 16*player` fill, `C2B9..C2D8` clear の 4 系統で、いずれも `C2F6` を含む `C2E0-C2FF` hidden shadow state を初期化していないため、`C2F6` producer は既知 zero-fill 群より別の bulk init / overlay / banked load 側にある可能性がさらに高い
- helper 契約の再確認では `0072` は `HL` base の `BC` byte zero-fill、`0067` は copy ではなく `HL*128 + DE` 系 address math helper、`00B5/00BC` は `RST $28` を前後に挟む banked copy wrapper と読むのが自然で、現時点の高確度 caller も主に `C200`, `C785`, `C760`, `A600` 周辺に偏っているため、`C2F6` producer はこの generic copy wrapper 群の visible/runtime caller とは別線にある可能性が高い
- `0198` の高確度 immediate caller は `6451` の shop/selector candidate scan 後、`62B6` の selector-budget cluster 内、`5E70` の UI/selector dispatch gate に集約でき、いずれも caller 直前では `C745`, `C207`, `FF9B`, `C796` など local scratch/visible runtime setup しか見えず `C2F6` 自体を埋めていないため、`C2F6` backing state は immediate caller より前段の wider shared init / mode transition / overlay reload で準備される可能性がさらに高い
- `5E0D-5E77` は `FF96`, `C797/C798`, `C7A6`, `CC00`, `C796`, `C7DA`, `FF9B`, `C34A` などを扱う shared mode-transition / UI-selector reset helper family とみるのが自然で、この範囲でも `C2F6` を direct に埋める clear/copy/write は未検出のため、`C2F6` backing state はこの family よりさらに前段の shared init / overlay reload / hidden import で準備される可能性が高い
- `01B0/01B3/01B6` は caller 文脈上それぞれ shared reset/wait-ready 前段、`5E31/5E35` reset family の共通 tail exit、selector/UI dispatch 直前 setup として分かれて見え、いずれも visible runtime state の reset/finalize/setup に寄っていて `C2F6` hidden backing state の producer には見えないため、`C2F6` 探索はこの family よりさらに前段の hidden/shared init に寄せるのが安全
- `5E0D` 後段の `5F2B/5F44/5F0E` も caller 文脈上は `RST $10` / `RST $18` 直後に走る visible refresh family とみるのが自然で、既報の `5F0E` は palette/VRAM 更新前同期候補でもあるため、`5E0D` 全体は visible mode transition routine としてかなり閉じており、ここでも `C2F6` hidden backing state の producer は未検出のまま
- `RST $08` は caller 文脈上 `E` code で command/message/selector entry を切り替える visible dispatch primitive とみるのが自然で、`5E6F/5E75` の base/resolved optional dispatch や `62BC/62EF/62FC` の candidate/success/wait dispatch で整合するため、`01B6 -> RST $08 -> 0198` は `prepare -> dispatch -> presence-check` の 3 段として分離でき、ここでも `C2F6` hidden backing state の producer は見えていない
- selector-budget cluster の `5E65(DE=$1C1B)` と `5EFE` は caller 対称性から success/fail event family とみるのが自然で、`RST $08(E=$1D/$1F/$29)` と組み合わさる visible event payload/state builder に属しているため、この層も `C2F6` hidden backing state の producer ではなく visible side effect として分離できる
- `C2F6` producer 探索の境界整理では、`006D/0072/00B5/00BC` の既知 high-confidence caller 群、`5E0D-5F44` の mode-transition/refresh family、`01B0/01B3/01B6 -> RST $08` の visible dispatch family、`5E65/5EFE` の success/fail event family のいずれからも `C2E0-C2FF` hidden block 準備線が出ていないため、今後は visible/runtime 層を原則固定し、`C2F6` を larger hidden shadow block の一部として hidden/shared init / overlay reload / banked import 側へ探索を寄せるのがもっとも筋がよい
- hidden/shared init 側の入口候補としては、優先順に `C2E0-C2FF` を含む larger hidden block init、visible `C200` transport とは別の overlay/banked import、`0198` caller より十分前段の mode-entry bulk setup、最後に runtime overlay alias を疑うのが自然で、以後の `C2F6` producer 探索はこの順で cluster を当てるのがもっとも効率がよい
- 具体 cluster で見ると、Aランクは `60C0-60E1` の `C21F` block/sentinel builder、`60E8-611B` の player-by-player orchestration loop、`611C-6156` の `C20F` fill + `C73D` seed + selector gate helper で、Bランクは `6157-61FF` の battle-side staging と visible `C200 <-> A600` 以外の banked import/export 残差、Cランクは runtime overlay alias 仮説となるため、次はこの順で hidden/shared init 候補を当てるのがもっとも筋がよい
- `60C0-6156` 全体を 3 段の hidden/shared seed layerとして見ると、`60C0-60E1` は packed-entry -> block/sentinel builder、`60E8-611B` は parent orchestration loop、`611C-6156` は player-local candidate/selector seed + gate helper に分かれ、apply/staging 色の強い `6157-61FF` より `611C-6156` のほうが `0198` backing state に近い可能性が高いため、次の最優先は `611C` 周辺の contract 化になる
- `611C-6156` の gate 部は `RST $08(E=$15)` による candidate/selector dispatch、`01B9 -> FF8C` による current selection resolve、`5F07` による selection validate/remap、`C73D + A -> 019E` による resolved seed byte consume/commit の 4 段として読むのが自然で、`0198` backing state に最も近い hidden-local seed helper 候補として引き続き最上位に置ける
- `019E` は現時点の高確度 caller 文脈では `611C` 後段の `C73D + remapIndex` から得た 1byte seed を受け取る resolved seed byte consumer / commit helper とみるのが自然で、visible dispatch/resolver ではなく hidden-local 反映点に近い契約として持つのが安全
- `5F07` は `01B9 -> FF8C` の直後に `611C` と `662B` の両方で呼ばれており、`611C` では `C73D` seed table index、`662B` では gate 判定用の戻り値として使われているため、current selection を caller-local domain の local index へ validate/remap する helper とみるのが自然で、`611C` の chain は `FF8C -> 5F07 -> C73D[index] -> 019E` としてかなり明確になった
- `01B9` は `611C`, `6621`, `62BE` の 3 系統で共通して `FF8C` を直後に読ませる current selection resolver とみるのが自然で、`FF8C` は final local index ではなく `5F07` に渡す前段 token (`0xFF` は no selection) として扱えるため、`611C` の入口側も `RST $08(E=$15) -> 01B9 -> FF8C -> 5F07` の chain としてかなり閉じてきた
- `RST $08(E=$15)` は `C20F + 16*player` の `FF` 初期化と `C73D..C744 = F0..F7` seed table 初期化の直後にだけ現れ、後段で `01B9 -> FF8C -> 5F07 -> C73D[index] -> 019E` が固定して続くため、単なる visible event code ではなく seeded local candidate set を current selection chain に接続する dispatch とみるのが自然で、`611C` の入口契約もかなり実装寄りに閉じてきた
- `C20F` は `611C` で `C20F + 16*player` を `FF` クリアした直後に seeded candidate gate の前提として使われ、`6332` でも `C204/C206` と同じ `019B` 経由 16byte stride family に属し、`10CC` high-range selector dispatcher でも `HL=$C20F` が見えるため、visible `C200` main record ではなく player-local selector/candidate work record family とみるのが自然である
- `C73D..C744` は現時点では `611C` 内で `F0..F7` に初期化され、そのまま `5F07` の返す local index で引かれて `019E` へ渡される 8要素 local seed/remap table とみるのが安全で、global shared table より `C20F` と対になる seeded candidate gate 専用の局所 table として扱うほうが整合する
- `607A-6087` には `C2DA` 先頭 byte を low nibble のみに正規化する in-place pass があり、`6087-60A2` には `C21F` block head を `((block+1)<<4) | lowNibble` 風に再構築する helper が見えている
- `60AA-60B7` は `C21F` の最初の 4 block head を `FF` で clear する隣接 helper とみるほうが安全で、`60B8-60E1` と同一処理に潰さないほうがよい
- `60E8-611B` は `player_index ($C709)` を 1..3 で回しながら `611C` と `6157` を呼ぶ player-by-player orchestration loop 候補
- `60E8-611B` を親ループとして読むと、`5F22/5E77` の前段 setup のあと各 player ごとに `611C` seeded gate と `6157` staging を起動し、失敗時は同じ parent loop 内で retry/rollback 分岐へ戻している形になるため、`C20F/C73D/FF8C` は `611C` 局所 scratch ではなく player-by-player に再生成される local seed pipeline state とみるのが自然である
- `5F22` は現時点の high-confidence callsite では `60E8-611B` 親ループ先頭にしか見えておらず、`5E77` より前・`C709` 初期化より前に一度だけ走るため、`611C/6157` のような per-player helper ではなく subsystem-entry の one-shot pre-loop setup / global precondition helper とみるのがもっとも安全である
- `5E77` も `60E8-611B` では `5F22` の直後、`C709` 初期化より前に一度だけ走るため per-player local gate ではなく、既報の `FF8B` 分岐と copy/update profile を合わせると mode-entry visible update / render-precondition helper とみるのが自然で、`5F22 -> 5E77 -> 611C/6157` は `global precondition -> visible precondition -> player-local seed/apply loop` の 3 層として分けて持てる
- `611C` は `C20F + 16*player` を `FF` 埋めし、`C73D..C744` を `F0..F7` で初期化してから `FF8C` ベースの選択/検証を行う helper 候補
- `6157` は `60E8-611B` 親ループ内で `611C` 成功後にだけ起動され、`C200 + 16*player <-> C7EE` の 4 byte scratch copy、`RST $08(E=$2C/$2D)`、`D400/D500` family 初期化を含むため、hidden producer 本体より per-player apply/staging 後段とみるのが自然であり、`5F22 -> 5E77 -> 611C -> 6157` は `global precondition -> visible precondition -> local seed/validate -> local apply/staging` の 4 層として持てる
- この 4 層モデルを `C2F6` 探索線へ戻すと、`hidden-init 候補` と `battle-side apply` の境界は `611C / 6157` の間に引くのが最も自然で、今後の producer 探索は `611C` を最上位、`60C0` をその前段 shared builder、`5F22/5E77` を one-shot precondition、`6157` を後段 apply layer として優先順位づけするのが安全である
- `611C` と `0198` はどちらも gate 系で近い層にあるが、`0198` は `C2F6` 系 table を読む shared optional-entry presence predicate、`611C` は `C20F/C73D/FF8C/5F07/019E` を束ねる player-local seed/selection gate とみるのが最も整合するため、今後は「`611C` をそのまま `C2F6` helper とみなす」より、`60C0` を shared builder 候補として両者の前段に置く線を優先するのが安全である
- `60C0-60E1` は `C2DA` packed entry から `C21F` block head と `C7E0..C7ED` sentinel list を構築する shared builder とみるのが自然で、`611C` local gate と `0198/C2F6` shared predicate の direct helper というより、両者のさらに前段で shared candidate universe を整える layer 候補として持つのが現時点では最も安全である
- `60C0` の 2 出力のうち `C7E0..C7ED` 側は `5B64-5B90` populate と bank0 `1237/109F` read path まで見えているため shared sparse remap/list としてかなり強い一方、`C21F + 16*block` 側は `+0` sourceIndex 以外の consumer がまだ弱く、今後 shared candidate-state builder 仮説を強めるには `C21F` の `+1..+F` consumer を優先して洗うのが最も効率がよい
- `5B95`, `60E2`, `10CC` 周辺で見えている `C21F` 参照は現状ほぼすべて「16byte block base を返す/使う」段に留まっており、`+1..+F` の richer field consumer はまだ直接取れていないため、次の主戦場は `C21F` base-return helper そのものではなく、その caller 直後で block 内 offset を読む path の抽出になる
- 現時点で `5B95` caller も `60E2` caller も `CALL ... ; LD (HL),C` までしか高確度に取れておらず、`10CC` も selector dispatcher として `C21F` block base/reference を正規化している段に留まるため、`C21F` richer struct 仮説を強める決め手は「base 取得後に `INC/LDI/offset add` で `+1..+F` を読む caller 直後 path」を見つけられるかどうかにある
- とくに `10CC` 後半で粒度が上がっているのは `C21F` 自体より `C20F/C71D/C2B9/C7E0` など selector source 側であり、現状 `10CC` を掘り続けても `C21F` richer field の証拠には直結しない可能性が高いため、次の探索単位は `C21F` path に限定した caller-after-base の offset read 抽出へさらに絞るのが安全である
- `C21F` について次に残すべき候補は、`CALL $5B95 / $60E2 / $10CC` や `LD HL,$C21F` の直後に `INC HL`, `LDI A,(HL)`, `ADD HL,offset` などで `+1..+F` を読む path だけであり、`CALL ... ; LD (HL),C` や `A=(HL); CP $FF ; JP $1551/1552/1554` のような block `+0` writer / selector terminal path は当面 richer-field 候補から外してよい
- 既存 high-confidence path を突き合わせた範囲では、現時点で `C21F + 16*n` の `+1..+F` を読む強い実例はまだ確認できておらず、`5B95/60E2` は block `+0` writer、`10CC` は selector dispatcher に留まるため、`C21F` は今のところ rich candidate-state struct より block-head/index table 仮説を一段強めて持つのが安全である
- 現時点では `C20F` は player-local selector/candidate work、`C7E0` は shared sparse remap/list、`C2F6` は shared optional-entry presence backing state とみるのが最も整合する一方、この 3 つがどこで直接交わるかはまだ見えていないため、次の主戦場は `C20F/C7E0` 側の横読みより `C2F6` producer を hidden/shared init 側から再度詰める線に戻すのが自然である
- そのうえで実務順を更新すると、`C2F6` producer に最も近い本命は依然 `611C-6156` の hidden-local seed/selection gate であり、`60E8-611B` はそれを起動する親ループ、`60C0-60E1` は shared builder 補助線として扱うほうが現在の evidence に整合するため、以後は `611C -> 60E8 -> 60C0` の順で優先するのが安全である
- `611C` をさらに分けるなら、`RST $08(E=$15)` と `CALL $5F0E` は candidate presentation / visible refresh 側の outer edge、`01B9 -> FF8C -> 5F07 -> C73D[index] -> 019E` は current pick を resolved seed byte へ落として commit する inner core chain とみるのが自然であり、`C2F6` producer に最も近い本命も後者の core 側にある可能性が高い
- その inner core chain の中でも、`01B9` は selection token materializer、`5F07` は local-index remap、`C73D` は seed source に留まるのに対し、実際に state mutation を起こす可能性が最も高いのは `019E` なので、`611C` を次に詰める優先順位は `019E` writeback 先 -> `FF8C` token domain -> `5F07` index domain の順に置くのが効率的である
- この観点で見ると `019E` は `611C` inner core における最初の state-mutation frontier とみるのが自然で、`01B9/FF8C/5F07/C73D` が token/materialize/remap/source の前段に留まるのに対し、`CALL $019E` 直後に `SCF ; RET` する caller 契約からも、次に観測すべき境界は `019E` direct body / writeback destination そのものだと固定してよい
- `611C`, `6621`, `62BE` を並べると、`FF8C` は final local index ではなく `0xFF` invalid sentinel を持つ current-selection token storage とみるのが最も自然であり、`5F07` 後に初めて caller-local な index 判定が可能になるため、`019E` が受け取るのも token ではなく `C73D[localIndex]` から読んだ resolved seed byte だと整理してよい
- 既存 high-confidence evidence の範囲では、`019E` の主 caller は依然 `611C` inner core に集中しているとみるのが安全で、別 subsystem の visible dispatch / render / selector terminal と直結する強い別 caller はまだ見えていないため、次に `019E` direct body / writeback destination を詰めるときも `611C -> 019E` の下向き探索を主線に据えるのが false positive を減らしやすい
- `019E` の writeback 候補を層で分けると、`611C` 内側の player-local hidden seed/selection state と `0198/C2F6` に近い shared optional-entry backing state を最優先に疑うのが自然で、`C7E0` sparse remap や `C7EE/C200/D400/D500` の `6157` 以降 apply/staging 層は優先度を下げてよいため、次の探索順も `local hidden -> shared backing -> C20F hidden field -> apply/staging` の順で持つのが安全
- さらに `611C`, `0198/C2F6`, 4 層モデルを重ねると、`019E` の first writeback は shared `C2F6` 直書きより local hidden commit 側に置くほうが整合的で、shared optional-entry presence はその後段 reflection として扱うのが false positive を減らしやすいため、次の探索では `019E -> local hidden result -> later shared presence` の順を基準線にしてよい
- この local hidden 仮説を `611C` の具体物へ落とすと、`019E` の immediate target は `C73D` source や `FF8C` token そのものではなく、`611C` local gate の中にある別の result slot とみるのが最も自然であり、`C20F` は近い候補ではあるものの現状は precondition/workspace としての証拠のほうが強いため、次の探索順は「未命名 local hidden result field -> `C20F` 内 hidden field -> later `C2F6` reflection」の順に置くのが安全
- 既存 evidence を消去法で重ねると、`019E` の immediate target として最も自然に残るのは `611C` inner core と同じ layer にある未命名 local result slot / shadow byte であり、`C73D` source・`FF8C` token・`6157` 以降の apply/staging 群は first writeback 候補としてかなり後退するため、今後の local 観測単位は `611C` core 周辺の未命名 local hidden state に集中させてよい
- `611C` core 周辺で現時点までに役割がかなり見えている named local family は `C20F` workspace、`C73D` seed source、`FF8C` token storage の 3 本にほぼ収束しており、この 3 本をそのまま immediate target に当てるにはどれも決め手が弱いため、次の first guess はこれらの外側にある未命名 local result/shadow family とするのが最も安全である
- その未命名 local result/shadow family も、`RST $08(E=$15)` / `5F0E` の outer edge や `6157` 以降の apply/staging 層ではなく、`01B9 -> FF8C -> 5F07 -> C73D[index] -> 019E` と同じ inner core 層に属するとみるのが最も整合的なので、今後の `019E` immediate target 探索は `611C` core 直近の unnamed local state に固定して進めてよい
- さらに時間順で見ると、その unnamed local shadow/result family は `FF8C` token と `5F07` remap で selection を解決し、`C73D[index]` から resolved seed byte を読んだ**あと**、しかし `6157` apply/staging に渡る**まえ**の post-resolve / pre-apply window に置くのが最も自然なので、次の探索単位も「resolved seed byte を local settled result にする small shadow slot」へさらに狭めてよい
- この small shadow slot も、単なる raw seed cache より `611C` の success を成立させる **settled local result / result marker** とみるのが自然であり、`019E` は resolved seed byte の単純保存より local outcome commit helper の色が濃いため、今後の探索も `A` の直書き先だけでなく inner-core settled-state 仮説を first line に置くのが安全である
- その settled-state 仮説も現時点では「単一 byte の result marker」に寄るものの、`019E` が `611C` 成功境界にいることを考えると `result byte + status` あるいは very small shadow tuple のような small family まで含めて持つのが安全であり、逆に full `C20F` record や `6157` 後段の big/apply struct は優先度を下げてよい
- size 観点でも `019E` immediate target を big struct / long record / apply-side page とみなす必要はかなり薄く、`C20F` を疑う場合も full 16byte workspace より hidden subfield として十分なので、次の探索単位は single byte / marker + status / short tuple のような short settled-state family に固定してよい
- その short settled-state family の意味も、単なる raw seed 保存より `result marker` か `resolved local outcome byte` のどちらかに寄るとみるのが自然で、どちらも `611C` 成功を成立させる意味付き short state という点で共通するため、今後の `019E` 観測線は `marker vs outcome` の二択を中心に持つのが安全である
- さらに高位では、この `marker` と `outcome` はどちらも `611C` 成功を成立させる **success-side short state** の下位バリエーションとしてまとめて持つのが安定しており、次に探すべきものも raw seed cache ではなく `success marker` / `adopted outcome byte` / `short success pair` としての local hidden state にかなり絞れる
- そのうえで `019E` が入力として受けるのは resolved seed byte なので、immediate target を pure boolean success flag だけとみるより `meaning-bearing byte` を含む short state とみるほうが自然であり、次の観測優先度も `adopted outcome byte` -> `marker + outcome byte` -> `marker only` の順に置くのが安全である
- さらにこの優先順位を意味論で言い直すと、`019E` immediate target は resolved seed を local meaning を持つ `outcome byte` へ落とす仮説を first line に置くのが最も自然で、`marker + outcome byte` は second line、`marker only` は third line とするのが安定しており、次の観測点も `resolved seed -> local outcome value` の写像有無に寄せてよい
- その写像有無については、現時点では `resolved seed -> local outcome byte` の mapping 仮説を first line、`resolved seed` の identity storage を second line に置くのが安全であり、`019E` が commit frontier にいることを考えると raw 値の単純退避より local outcome semantics を帯びた short state とみるほうがやや自然である
- この重み付けをもう一段明示すると、`019E` immediate target は success-side local meaning を持つ 1byte state とみるのが最も安定しており、identity 仮説が成立する場合でもそれは単なるコピーではなく「今回の成功側結果として有効化された値」として読むべきなので、今後の観測線も `outcome-semantics bias` を first line、identity storage を second line に置いてよい
- さらに言えば `resolved seed` の identity storage は `outcome mapping` の恒等写像ケースとして吸収できるため、実務上の観測軸は `identity vs mapping` より **success-side local meaning を持つ 1byte state かどうか** に一本化してよく、次の探索も adopted/selected outcome に読める local byte の観測を first line に据えるのが自然である
- 移植観点で残る最小 blocker は、`019E` が確定する success-side local meaning と、それが battle 本線のどこで読まれて RNG slot 消費順や action resolve 中間 state にどうつながるか、という 2 本にかなり収束しているため、次の最優先も `019E` 後段 local state の battle-side consumer を取る線でよい
- その consumer 探索も、`D400/D500` や `C200` の wide page 全体を掘るより、`611C success -> 6157 entry -> battle action resolve` の narrow bridge を優先したほうが移植 API へ直結しやすいため、次の主戦場は success-side local state を action resolve 入力へ渡す狭い handoff 線として持つのが安全である
- この narrow bridge の中でも最小の観測点は `60F3: CALL $611C ; 60F6: JR NC ; 60F8: CALL $6157` でできる `611C success -> 6157 entry` の handoff 境界であり、次に battle-side consumer を取るなら `6157` の wide page side effect 全体より「entry 時点で何がすでに local success state として決まっているか」を先に押さえるのが最短である
- その `6157` entry 契約も現時点では `current player (C709)` と `611C` が直前に確定した local success-side state を最小 guaranteed input とみるのが安全で、`C200/C7EE/D400/D500` は entry 入力より `6157` がこれから生成する staging state として後段に回すほうが整理しやすいため、次の consumer 探索も「player + local success state」を受け取る狭い入口契約の確認に寄せてよい
- さらにこの入口契約は shared/global より current player に束縛された **player-scoped local success handoff** とみるのが最も自然であり、親ループも `C709` を更新しながら `611C -> 6157` を 1 player ずつ直列実行しているため、次の consumer 探索も 1 player 分の narrow bridge として見るのが安全である
- 残り要素の実作業順もかなり整理できていて、最短線は `019E` immediate/local success state の形 -> `6157` entry の player-scoped handoff -> battle action resolve consumer -> RNG slot 消費順 -> action descriptor struct の順で持つのが自然であり、いま最も効く次の一手は `6157` entry が local success state をどう前提にしているかをさらに詰めることになる
- `6157` entry をさらに細く見ると、事前に存在するとみてよいのは `current player (C709)` と `611C` が直前に確定した local success-side state だけで、`C200/C7EE/D400/D500` は `6157` がその local state を battle-side へ展開する過程で初めて大きく構築・更新する staging state とみるのが自然なため、次の consumer 探索も entry 前に既にある local success-side state の形を本命にしてよい
- その local success-side state の最小 handoff 形も現時点では `playerIndex + 1byte outcome-like state` を first line、`playerIndex + short pair` を second line に置くのが最も軽くて整合的であり、`6157` entry 契約もまずは `(player, outcomeLikeByte)` を受ける narrow bridge としてみるのが安全である
- この narrow bridge を高位で見ると `6157` は `playerIndex + outcomeLikeByte` を battle-side apply 意味へ昇格させる player-scoped outcome relay とみるのが自然であり、`C200/C7EE/D400/D500` はその relay の内部 staging として後段に回せるため、次の consumer 探索も `outcomeLikeByte` が最初にどの battle-side 意味で読まれるかの確認に寄せてよい
- その `outcomeLikeByte` の first consumer 意味も現時点では page-wide record 全体より `1player` 分の action resolve を進める branch/input meaning に寄るとみるのが自然であり、次の観測線も `D400/D500` 全 field の前に「この 1byte が adopted result / selected branch code / success-qualified local result のどれに近いか」を見るのが安全である
- この 3 候補の中では、いま一段だけ重みを置くなら `selected branch code` を first line、`adopted result byte` を second line に置くのが自然であり、`6157` が player-scoped outcome relay である以上、その直後の consumer は large record 解釈より「この player を次にどの処理枝へ進めるか」の 1byte meaning をまず必要とする可能性が高いため、次の観測も branch/input meaning の確認へ寄せてよい
- その `selected branch code` も現時点では wide page selector より `1player` 分の local action resolve を前へ進める局所 code とみるのが自然であり、次の consumer 観測も「どの page に落ちるか」より「どの局所分岐・どの apply mode を開く 1byte か」を本命にして進めるのが安全である
- この局所 code の内訳では、現時点では `apply-mode selector` より `local action-path selector` に少し重みを置くのが自然であり、`6157` が内部で大きな staging を始める前にまずその player の結果をどの局所枝へ送るかを決める必要があると読むほうが narrow bridge 仮説と整合するため、次の観測も `branchCode` が最初にどの局所分岐を選ばせるかの確認を first line に置いてよい
- さらにその局所分岐の型では、単発の `hit/miss` や `adopt/reject` の即時結果分岐より、`6157` 以降でその player を次にどの小段階へ進めるかを示す `next local phase selector` とみるほうがやや自然であり、次の consumer 観測も「この 1byte がどの局所 phase 遷移を開くか」を first line に置くのが安全である
- この `next local phase selector` を battle 側 consumer の観点で言い直すと、`6157` 後段で最初に必要なのは `D400/D500` 全体の構築より `outcomeLikeByte` がどの local action branch / next phase を開くかという first branch decision であり、移植でもまず `decodeResolvedOutcome -> advanceActionPhase` の中間 API を立てるのが筋がよい
- 現時点の narrow core 境界としては、`019E` が確定した `outcomeLikeByte` を `decodeResolvedOutcome(player, outcomeLikeByte)` で branch 化し、その branch を `advanceActionPhase(player, branch)` へ渡す 2 段で持つのが最も自然で、次の解析目標も storage address より `branch` 意味の確定に置くのが安全である
- その `branch` の粒度も現時点では battle round 全体の大 phase より `1 player` 分の local action resolve を一段先へ進める per-actor local resolve step とみるのが最も自然であり、次の consumer 探索も global phase や UI state ではなく actor-local な phase/step consumer を本命にして進めるのが安全である
- したがって次の実観測点も `player + branch` を入力に取る actor-local consumer 境界へかなり絞れており、wide page 更新や round 全体 manager より先に、current player に束縛された local action branch opener を探すのが最短になる
- step 4/5 を移植 API として見ると、`D?43-46` / `C1A5-C1AC` は `BattleActionHead(kindId, arg, target, slotIndex)` の first-line struct として先に固定でき、RNG slot 消費順はその head と `branch` を最初に同時に読む actor-local opener の内側へ狭めて探索するのが最も安全である
- この actor-local opener を既報の battle-side evidence に重ねると、本命 frontier は `6157` 本体より `actors` queue / local record consumer 側にあり、first RNG use は現時点では slot `07/08` による candidate-selection 系、first ActionHead use は `target/slotIndex` より `kindId/arg` に重みを置くのが最も自然である
- step 6 に向けた front/core 境界もかなり具体化できていて、Godot 側は first-line では `actorIndex + BattleActionHead(kindId, arg, target, slotIndex)` を TypeScript へ渡し、TypeScript 側は `branch` と `target` を返す `resolveActorCommand(input)` 型の actor-local bridge から実装を始めるのが最も安全である
- `resolveActorCommand(input)` の first version 実装順もかなり見えていて、まず `kindId/arg` を local path/class selector として解釈し、その path が必要とする場合だけ slot `07/08` の candidate-selection RNG を使い、`target/slotIndex` routing へ進めるのが安全である。いっぽう hit/damage 系 RNG は `07/08` の直後と決め打ちせず、未確定の別 frontier として切り離して持つほうがよい
- したがって step 6 に向けた first-version core では、slot `07/08` は candidate-selection 専用で実装し、hit/damage/order 系 RNG は `resolveCombatRng(...)` 相当の extension point に逃がすのが最も正確である。次の本丸は `07/08` の精密化より、この別 combat RNG frontier の新規発見になる
- この別 frontier を探う観測点もかなり絞れていて、本命は `6157` 後段の actor-local opener 内、特に `actors` queue 分岐先で `07/08` ではない raw/small-range RNG (`FF00`, `0300`, `0F00`, `0100`) を要求する callsite である。つまり次の battle/RNG 解析は `resolveCombatRng(localPath, routedTarget, ...)` に相当する hook を探す段階に入っている
- さらに優先 window を battle state machine 上で絞ると、round aggregate に見える state `06/07` より、current actor page と pointer-like record を扱う state `04/05` 周辺、特に `actors` loop の `41A4-41F1` 帯を first-line の combat RNG 観測点に置くのが最も自然である
- この `04/05` priority window は、既報の `branch = next local phase selector` 仮説とも整合しており、`decodeResolvedOutcome(...)` が開く actor-local phase の first-line 候補として扱える。したがって次の本命は `04/05` 帯の内側で `resolveCombatRngAfterLocalPath(...)` に相当する narrow hook を探すことになる
- `04/05` 帯の中でも優先順位はさらに付けられていて、state dispatch 自体 (`41A4-41B4`) より、pointer/record materialization の後に sentinel / special code / 3byte-table / decrement 更新が並ぶ `41C4-41EC` を first-line の combat RNG resolution belt、`41F1-4205` を second-line の post-local gate として扱うのが最も自然である
- さらに `41C4-41EC` 自体も、sentinel / special code 判定中心の `41C4-41D8` pre-filter と、3byte-table entry を引いて decrement/writeback する `41D9-41EC` consume/update belt に分けるのが自然であり、raw/small-range RNG の本命 hook は後者 `41D9-41EC` へ一段寄る
- その `41D9-41EC` も、`FF` sentinel を見ながら `index*3+0x14` の local table entry を解決する `41D9-41E5` と、counter byte を読んで `FE` sentinel で分岐し `DEC/writeback` する `41E6-41EC` に分けるのが自然であり、combat RNG の本命 hook は後者の counter consume belt にさらに寄る
- さらに `41E6-41EC` も、counter read (`41E6`)・sentinel gate (`41E7-41E9`)・consume/writeback (`41EB-41EC`) に分けるのが自然であり、combat RNG の本命 hook は `DEC` そのものより `sentinel gate -> local consume policy` を決める narrow slot に寄る。したがって次の本命は `41E7-41E9` 相当の local decision slot である
- ここまでの narrowing を battle core API へ戻すと、最初に回収できる combat RNG の意味は damage/hit そのものより `shouldConsumeCounter` 的な local consume/accept policy に寄るとみるのが最も自然であり、次の解析もこの small decision bit を first recovered semantics として確認する方針でよい
- この first recovered semantics は TypeScript 側でも `ActorResolveResult` の optional `combatDecision` として先に持たせるのが自然であり、`branch + target` に加えて `shouldConsumeCounter` 的な small decision bit を返せるようにしておくと、battle 本線の実探索と core 実装をずらさずに進められる
- したがって次の battle/RNG 解析で本当に確認すべき点も 2 つに絞れていて、`41E7-41E9` 相当の local decision slot に raw/small-range RNG が入ることと、その返り値が `41EB-41EC` の consume/writeback 可否へつながることを押さえれば、`combatDecision.shouldConsumeCounter` を first recovered combat semantics としてかなり強く採用できる
- ここまでの narrowing を step 6 の実作業順へ戻すと、battle core の first working skeleton は `branch/path -> candidate RNG -> combatDecision` の 3 段で持つのが最も自然で、`resolveActorCommand(...)` の内側では `combatDecision` だけを最後の provisional hook として残せば front/core/ROM 解析を同じ順番で進められる
- したがって step 6 の current milestone は、`BattleActionHead` / `branch` / `localPath` / `slot07_08` / `target routing` を先に実装し、`combatDecision` だけを unresolved hook に残した actor-local bridge を成立させることにある。Godot 側もこの cut なら `branch/target/debug` ベースで先に接続を始められる
- この cut まで来ると step 6 はもう「解析完了待ち」ではなく「skeleton 実装開始」の段階に入っている。現時点で safest な coding boundary は `resolveActorCommand(...)` で、未確定は `resolveCombatRngAfterLocalPath(...)` すなわち `combatDecision` hook 1 点へかなり集約されている
- さらに current skeleton では、`resolveActorCommand(...)` が `branch / localPath / target / targetSource / candidateOffset / combatDecision / debugTrace` を返す debug-oriented actor-local bridge として実装済みであり、Godot 側でも `ATK / DEF / ABL{index}` の command matrix preview を通じて `kindId/arg -> localPath -> optional 07/08 candidate RNG -> target routing -> combat hook` の流れを front から直接観測できる
- この command matrix には debug 専用 `PTR` probe も加わっていて、`kindId=0x01, arg=0, target=0xFF` を使って slot `07/08` candidate-selection path を front / self-check の両方から強制観測できる。したがって current step 6 では `07/08` 経路はかなり固定されており、主な unresolved point は `combatDecision` へさらに集中している
- 現在の `combatDecision` は単なる `false` stub ではなく、`pendingWindow = 41E7-41E9 -> 41EB-41EC` と `pendingMeaning = local_counter_gate | candidate_counter_gate` を返す provisional frontier annotation として持っている。つまり unresolved hook 自体にも、どの ROM narrowing に対応するか・どの local path 分類に属するか、という最小限の意味が既に付与されている
- さらに `actors_loop` 既報の exact flow (`41D9-41EC`) を見ると、`41E7-41E9 -> 41EB-41EC` 自体は `counter read -> sentinel gate -> DEC/writeback` の deterministic consume belt に近く、RNG があるとしても `DEC` の中ではなく **この consume path に入る branch 条件の形成側** にあるとみるほうが自然である。したがって `combatDecision` は「counter を減らす処理そのもの」ではなく、「その deterministic consume path を開く gate」を切り出したものとして持つのが安全である
- その branch 条件形成側もさらに見ると、`41C4-41D8` にある `C == FF/0E/0F` と `H == 0/1+` の special/sentinel 分岐群が `41D9-41EC` への進入を切り替えているため、current best reading では `combatDecision` の真の source は `41E7-41E9` 単独より **`41C4-41D8 -> 41D9-41EC` の連結 pre-gate** にある可能性が高い。次の decisive point は、この pre-gate 群に raw/small-range RNG が流入しているかどうかである
- さらに `41C4` に入る `C` と `H` 自体も直前 `41BC-41C3` で actor page から読まれた隣接 2byte pair と見えており、current best reading では `C = primary candidate/type code`, `H = secondary qualifier / owner / count 風 byte` と置くのが最も自然である。したがって next frontier は `41E7-41E9` 単独より、**この `C/H` pair を使う pre-gate 群の意味と、その pair 生成元に RNG が流入しているか** にさらに寄っている
- 既報 field 対応を重ねると、この `C/H` pair は `D?43-46` compact action head そのものより **`D?12..` repeated candidate entry family** に寄せて読むほうが整合的である。したがって current best reading では、`combatDecision` の unresolved source は `BattleActionHead` 直下ではなく、`branch/path` が開いたあとの **candidate-entry consumption / special-code gate** 側にある可能性が高い
- さらに current best provisional shape としては、`D?12..` の 2byte entry を **`{ code, qualifier }`** と置くのが最も自然であり、`code` は `FF/0E/0F` を含む candidate/type code、`qualifier` は `H==0/1+` の secondary gate field と読むのが安全である。これにより `combatDecision` は `action head` そのものではなく、**candidate entry の code/qualifier pair に対する local gate** に対応づけられる
- この `code` の中でも current best reading では `FF` と `0E/0F` を同列の sentinel とみなさないほうがよく、`FF = empty/end/invalid candidate`、`0E/0F = qualifier-aware special candidate subtype` と分けるのが evidence に最も合う。したがって次の decisive point は `41E7-41E9` 単独ではなく、**`special candidate gate (0E/0F + qualifier)` がどこで形成され、そこへ RNG が流入しているか** の確認にさらに寄る
- さらに current best reading では、`0E` と `0F` は完全に別意味の magic number というより **同じ special-candidate family の非対称 variant** とみるのが最も整合的で、`0E` は常に qualifier-aware path を通る strict variant、`0F` は `qualifier==0` のときだけ `41D9` へ直進できる zero-fast-path variant と置くのが安全である。これにより `combatDecision` は `counter gate` 単体より **special-candidate family gate** として持つほうが battle API に近づく
- この qualifier の中でも current best reading では `qualifier == 1` は単純な false bit より **special-candidate family における blocked / reserved ordinal** とみるのが最も自然である。`0` は fast-path 許可、`1` は reject、`2+` は非 fast-path だが consume 継続、という 3 区分が evidence に最も合うため、`combatDecision` の unresolved source も単なる counter 判定より **blocked-ordinal aware local gate** にかなり寄っている
- さらに `0E` と `0F` の対称性で見ると、`0E` は常に `41D5-41D7` を通り、`0F` も `qualifier != 0` のときは同じ `41D5-41D7` を共有するため、`qualifier == 1` reject は **`0E/0F` family 共通の blocked-ordinal rule** とみるのが最も自然である。非対称なのは `0F` だけが `qualifier == 0` で `41D9` へ直進できる点で、これは family 共通 blocked rule の上に乗る `0F` 特有の zero-fast-path として扱うのが安全である
- この `qualifier == 0` も `0E/0F` の両方で最終的には許可側に属するが、**同じ意味で許可されるわけではない** とみるのが current best reading である。`0E + 0` は strict path を通る `allowed zero`、`0F + 0` は `41D9` へ直進する `zero-fast-path privilege` とみるのが最も整合的で、`combatDecision` も単なる family-common gate より **family rule + `0F` variant privilege** の構造で持つほうが battle API に近い
- いっぽう `qualifier >= 2` については、現時点で `2`, `3`, ... の内部差を示す高確度 evidence はまだ見えていないため、まずは **`0E/0F` family 共通の nonzero-consumable class** として持つのが safest である。したがって current frontier では `0/1/2+` の 3 分類、すなわち `allowed zero` / `blocked ordinal` / `consumable nonzero class` までを押さえれば十分であり、`combatDecision` も単なる counter 判定より **special-candidate family の local accept policy** にかなり寄っている
- さらに current best bias では、この `0E/0F` family は storage 上は `D?12..` repeated entry family に乗っていても、battle-side meaning としては inventory raw item そのものより **action-path candidate layer** に寄せて読むほうが自然である。つまり `combatDecision` も `inventory use/count rule` より **action-path candidate accept policy** として持つほうが、`BattleActionHead -> localPath -> optional 07/08 candidate RNG -> combatDecision` という current skeleton に最もよく噛み合う
- ここまでの current shape をまとめると、`combatDecision` は now-currently **special-candidate family の local accept policy** として持つのが最も自然であり、その中身は `family-common accept/reject`、`qualifier==1 blocked ordinal`、`0F + qualifier==0 zero-fast-path privilege`、`qualifier>=2 consumable nonzero class` の 4 層まで押さえられている。したがって unresolved hook は単なる stub ではなく、すでにかなり battle semantics 寄りの provisional shape を持っている
- この current shape を consumer 側へ戻すと、`combatDecision` の first consumer は `D400/D500` の wide page staging 全体ではなく、**actor-local opener 内の pre-candidate gate / accept-policy branch** にあるとみるのが safest である。つまり next frontier は source refinement だけでなく、`kindId/arg` で local path を開き、`07/08` candidate-selection や special-candidate family gate を受ける narrow branch opener を押さえることに移っている
- さらに current best ranking では、この first consumer は **next local phase selector** より **candidate accept/reject branch** に一段重みを置くのが自然である。`combatDecision` の current shape 自体が blocked ordinal / zero-fast-path privilege / consumable nonzero class という accept-policy 寄りの semantics を持つため、first output も wide phase 名より「この candidate を通すか」「strict path に残すか」「shortcut へ進めるか」を決める narrow branch とみるほうが battle API に近い
- この ranking を output 順へ落とすと、current best reading では `combatDecision` の first consumer は **`accepted` を first output、`branch` を second output** に持つのが最も自然である。つまり current frontier は `branch first` より **`accepted first, branch second`** にかなり寄っており、`combatDecision` は branch 名そのものというより local accept/reject policy を経て local branch を具体化する layer として扱うほうが安全である
- さらに `accepted=false` の意味も、現時点では即座の battle-wide hard reject より **candidate reject with local fallback** とみるのが safest である。`0F` zero-fast-path と `0E` strict path の非対称性を踏まえると、false 側は「この candidate を通さない」「shortcut を閉じて strict/alternate local path へ落とす」という local opener 内の fallback と読むほうが現状 evidence に近い
- この fallback の current best ranking では、**alternate candidate path** より **strict path 残留** に一段重みを置くのが自然である。`0F + 0` だけが zero-fast-path privilege を持ち、`0E` や `0F + nonzero` は strict/slow-path 側へ残るという family 非対称性が見えているため、`accepted=false` の first-line 意味も「shortcut denied -> strict path retained」と読むほうが safest である
- さらに current best reading では、`accepted=false` のときは **`41D9-41EC` の current consume belt 自体をかなり高い確率で回避する** とみるのが自然である。`combatDecision` は belt の内部ではなくそこへ入る pre-gate に対応づけられているため、false 側は「current candidate consume path を開かない」「strict/alternate local fallback へ落ちる」と持つほうが整合する。したがって current frontier では、false 側は `strict-path` bias と同時に **consume-bypass** を first line に置ける
- この consume-bypass 読みは current best bias では `ATK` と `PTR` の両方に共有される。`PTR` は `07/08` で candidate source を組む点が違うだけで、最終的にかかる unresolved gate は同じ special-candidate family accept policy とみるのが自然なので、false 側は path-specific combat policy より **shared consume-bypass policy + path-specific candidate source** として持つのが safest である
- この前提をさらに詰めると、`PTR` false 側の fallback も current best reading では **strict-path** に一段重みを置くのが自然である。違いは false 側 policy 自体より `PTR` が candidate source を経由している点にあり、fallback の種類そのものより **fallback entry flavor** が path-specific とみるのが safest である。したがって current frontier では `ATK/PTR` を shared false-side policy で束ねつつ、必要なら `fallbackEntryFlavor` だけを candidate/local で分けるのが安全になる
- さらに current best bias では、`PTR` false 側の strict-path fallback で **即座に `targetSource` を再計算する** より、まず **candidate flavor を保持したまま strict path に落ちる** とみるのが safest である。したがって `PTR` false 側は `fallbackKind="strict-path"` に加えて `fallbackEntryFlavor="candidate"` を持つ、という形まで仮置きできる
- したがって step 6 の current frontier は「bridge を作ること」自体ではなく、bridge の unresolved field である `combatDecision` を ROM 側 evidence (`41E7-41E9 -> 41EB-41EC`) によって実意味へ押し上げることに移っている
- `6157` は `C200 + 16*player` と `C7EE` の 4 byte scratch copy を含み、さらに `D400/D500` family を巻き込む後段 staging helper 候補
- `61EB-61FB` に `C7EE -> C200 + 16*player` の逆向き copy があるため、`C7EE` は `60C0` builder の flat sentinel list ではなく player-local scratch header とみるほうが安全
- `405C-4066` には `CALL $0198` と条件付き `D400/D401` clear があり、従来の「単なる phase switch」より一段具体的な cluster 中継部だった
- ただし現時点では `D849/D949/DA49` の direct readback は未確認で、隣接する `D84D + 2*n` lookup 系とは分けて扱うほうが安全
- `45A8` は `DE` source から 2 byte pair を `HL` 側 page-stride で書く generic scatter writer に見え、`D849` family の direct consumer とみなす根拠は弱い
- `4361` dispatch 先と `449A-44F3` 側からも、現時点では `D849/D949/DA49` の direct readback は未確認

したがって、これは単純な seed 値保存域ではなく、lookup/permutation 型の補助表である可能性が高いです。

## 現状

乱数ルーチン単体の実行フローはまだ未確定です。ただし、battle 側の仮説だけだった状態から、少なくとも ROM 上に専用 256 byte table があることは確認できました。

- ダメージ算出で `rng & 0x03` 相当の分散を使う仮説がある
- 乱数値そのものは table 参照や index 更新を伴う可能性がある
- 少なくとも 1 本は RAM seed byte -> `data_rng` lookup の形が見えている
- `random_seeds` への direct 参照は現時点で `00:0258` と `00:0440` の 2 箇所のみ確認
- direct call/jp は `00:0440` ではなく `00:0469` に集まっている
- ただし `0469` は RNG API ではなく、待機/入力制御の key repeat ルーチンとして読むほうが自然になった
- したがって `0440` と `0469` は ROM 上で隣接していても、論理的には別責務の可能性がある
- `0CC4` 周辺は fresh input wait の高位 API と見なすほうが自然
- `043E/0469/049D` は usage handler 側の dispatch table に載るため、battle 専用 helper と断定する根拠は弱い
- `RST $28 -> 04B1` は ROM bank swap helper で、`043E` の `0F:4000` 参照を支えている
- したがって `043E` の一般ケースは unbiased scale より `raw % span + lower` に近い可能性がある
- `02F0-0306` は bank0 arithmetic helper 帯として読める
- `068F` は `RST $10` と `RST $18` を下位 primitive に持つ高位 wait/update/OAM flush routine と見なせる
- `043E` の slot は少なくとも単一用途ではなく、caller cluster ごとに使い分けられている可能性が高い
- battle 側には少なくとも slot `07/08` に加えて slot `33` caller が存在する
- ただし slot `07/08` も pointer / position offset 生成寄りで、damage core とは別責務の可能性がある
- ただし slot `33` は battle logic 本体より演出 particle 側に属する可能性が高い
- 既存 pass21-23 の damage 主線候補では `016B/043E` が未検出で、core damage / hit 用 RNG slot は別系統の可能性が高い
- pass19 の高スコア routine には data 誤読や effect/setup 側候補が混ざり、通常攻撃 entry の起点としては弱い
- bank `0D:40E6` は `battle_turn` / `actors` / `current_actor_index` を扱う battle runtime 入口候補で、通常攻撃探索の起点として有望
- `435A/4361` はこの battle runtime 入口配下の state helper と読むほうが自然で、RNG helper と混同しないほうが安全
- `4178-424D` は `actors` queue を走査しつつ state `04/05/06/07` と `435A` 集計を使う action resolve 前処理候補
- `01D4-01E3` は固定 inline operand つき `04BF` wrapper 群で、battle controller 側の descriptor dispatch 群に見える
- `437E` も RNG 本体ではなく、battle round 開始時の actor state / candidate 展開側に見える
- `4579` も RNG 本体ではなく、`actors` queue を構築する battle controller 前処理側に見える
- `443B-4499` は battle 側が slot `07/08` 指定で RNG を 2 回呼ぶ最初の具体的接続部として有力
- したがって battle 側での RNG の役割は、少なくとも一部で direct damage 値ではなく pointer/record 候補選択にある

## 実装方針

最初は ROM 完全再現よりも、注入可能なインターフェースで始めるのが安全です。

- `next(): number`
- `peek?(): number`
- `fork?(seed): Rng`

ROM 実装が確定したら、内部を table-driven 実装に差し替える。

## battle 側との境界

- `battle` は RNG 実装詳細を知らない
- `rng` は seed 更新と値供給だけを担当する
- ROM 実機再現が進んだら実装を差し替えられる形にする

## 追加解析の優先度

1. damage / hit 本体で使う slot を別系統で探す
2. `445E` ループの実初期レジスタと `BC` record ベースを確定する
3. index / seed 更新規則の確定
4. 行動順 / 命中 / ダメージ / ドロップでの消費順
5. `443B` caller を追って slot `07/08` の record writer を確定する
6. `D933` / `5FBC/60D3` を追って particle 初期化 API を固める

## Current CombatDecision Frontier

- current best reading では `combatDecision` は単なる counter gate ではなく、special-candidate family の local accept policy にかなり寄っている
- qualifier semantics の safest reading は  
  - `0` = allowed zero  
  - `1` = blocked ordinal  
  - `2+` = consumable nonzero class  
 であり、`0F + 0` だけが zero-fast-path privilege を持つ
- first consumer は wide staging ではなく actor-local opener 内の narrow accept/reject branch に置くのが自然で、output order も `accepted` first, `branch` second に重みがある
- `accepted=false` の first-line meaning は current consume belt (`41D9-41EC`) を開かず、strict-path 側へ残る local fallback とみるのが safest である
- この false-side consume-bypass policy は current best bias では `ATK` と `PTR` で共有され、違いは policy そのものより candidate source / fallback entry flavor に寄る
- `PTR` false 側は immediate `targetSource` recompute より、まず `fallbackEntryFlavor="candidate"` を保持した strict fallback とみるのが自然である
- さらに current best bias では、この candidate flavor は `targetSource` ラベルだけでなく `candidateOffset` まで含む **offset-aware candidate entry** として first fallback step に残る可能性が高い
- そして current best reading では、この `candidateOffset` は false 側でただちに target routing 専用になるより、まず **strict fallback branch generator を形作る selector material** とみるほうが自然である。したがって `PTR` false 側の unresolved 部分は `targetSource` 再計算より **branch choice shaping** に重みを置いて観測するのが安全である
- さらに current best bias では、この `candidateOffset` は branch choice を曖昧に支えるだけでなく、**false-side branch code の variant 差を first-line で形作る材料** とみるのが自然である。したがって `PTR` false 側で次に見るべき差分も final target routing より **branch code shaping** にかなり集約できる
- そして current best reading では、この branch code shaping の最も自然な現れ方は、`PTR` false 側が `ATK` false 側より **細かい false-side branch variant 空間** を持つことだとみられる。したがって次の decisive point も、policy 差そのものより **false-side branch-space の広がり** を見ることにかなり絞れる
- さらに current best bias では、この branch-space 拡張は mere candidate provenance ではなく、**`candidateOffset` の値域差が false-side branch variant を刻む** ことで説明するのが最も自然である。したがって次の decisive point は、`PTR` false 側の branch 差が offset 値域区分に対応するかどうかへかなり集約できる
- そして current best reading では、その offset 値域区分も raw large index を直接作るより、まず **small ordinal 的な false-side branch selector** に畳まれるとみるのが自然である。したがって次の decisive point は、offset 値域差が少数の branch ordinal bucket に写るかどうかへかなり集約できる
- さらに current best bias では、その small ordinal bucket も top-level `branch` そのものを置き換えるより、**strict fallback branch の下位 variant** を刻むとみるのが自然である。したがって `PTR` false 側の問いは `branch` を変えるかより、**branch の内側でどの variant を開くか** へかなり集約できる
- そして current best reading では、この `branchVariant` 相当の細分化は `ATK` false 側にはまだ不要で、**PTR false 側だけが optional な strict-fallback refinement を持つ** とみるのが自然である。したがって current provisional shape も `branch` を shared に保ちつつ、`branchVariant` は first-line では PTR-only optional field として扱うのが安全である
- さらに current best bias では、この PTR-only `branchVariant` も大きい fan-out ではなく、まず **2-way か少数段階の small-cardinality refinement** とみるのが自然である。したがって next step では `branchVariant` の有無だけでなく、その variant 空間が first-line でどれだけ狭いかを見るのが安全である
- そして current best reading では、その `branchVariant` を支える `candidateOffset` 側の値域 bucket も first-line では **実質 2-way split** とみるのが最も自然である。したがって current provisional shape も `branchVariant?: 0 | 1` にかなり寄せてよく、より細かい差分は second-line に遅延させるのが安全である
- さらに current best bias では、この `candidateOffset` の 2-way bucket は別の hidden selector を経るより、**そのまま `branchVariant 0/1` へ direct mapping される** とみるのが最も自然である。したがって first-line の provisional shape では `branchVariant = candidateOffsetBucket` と置くのが safest である
- そして current best reading では、この `branchVariant` は routing と並列に決まるより、まず先に確定し、その後に strict fallback の後段 routing が走るとみるのが自然である。したがって current consumer order も **`branch` -> optional `branchVariant` -> post-branch routing** にかなり寄せてよい
- さらに current best bias では、この post-branch routing は raw `candidateOffset` を再び直接読むより、**`branch` と optional `branchVariant` の結果だけを受けて進む** とみるのが自然である。したがって `candidateOffset` は first-line では `combatDecision` 内部の unresolved material に閉じ込め、後段へは `branchVariant` という要約形だけを渡すのが safest である
- そして current best reading では、その post-branch routing も `ATK` と `PTR` で完全分離するより、**shared core routing + PTR-only optional refinement** とみるのが自然である。したがって current provisional shape は `accepted + shared branch + optional PTR-only branchVariant` のままかなり直接後段へ渡してよい
- 現在の code frontier でもこの読みはかなりそのまま反映できていて、first-line では `combatDecision = { accepted, branch, branchVariant? }`、その後段に `postBranchRoute = routeAfterDecision(branch, branchVariant?)` を置く 2 層分離がもっとも自然である。したがって raw `candidateOffset` は first-line では `combatDecision` 内部 material に閉じ、後段には `branchVariant` という要約形だけを渡すのが safest である
- この 2 層分離は battle-side evidence ともかなり整合しており、current best reading では `combatDecision` が担うのは accept/reject と local branch choice まで、target/pointer/materialization はその結果を受ける routing layer に置くのが自然である。したがって current frontier では **decision layer (`accepted/branch/branchVariant`) -> routing layer (`postBranchRoute`)** を first-line decomposition として採るのが安全である
- さらに current best reading では、target routing / pointer materialization は routing layer の **second-line evidence** に置くのが自然である。したがって current implementation order も `combatDecision -> postBranchRoute -> target/pointer materialization` と積むのが safest であり、decision layer の独立性を保ったまま後段を詰められる
- この second-line evidence の現れ方も current best reading ではかなり明確で、`ATK` / `PTR` とも first-line では `postBranchRoute` までを先に確定し、そのあとで初めて `target` / `targetSource` / `pointer` 系の reopening が起きるとみるのが自然である。したがって current frontier では target evidence を `combatDecision` へ押し込むより、**postBranchRoute 後の second-line reopening** として扱うのが safest である
- さらに current best bias では、`PTR` false 側の second-line reopening 入口では **`targetSource="candidate"` の flavor がまだ維持される** とみるのが自然である。したがって `PTR` の candidate 由来差分は first-line では `branchVariant` に要約されつつ、second-line では candidate-flavored target/source evidence として reopening すると読むのが safest である
- そして current best reading では、この candidate-flavored second-line reopening も `targetSource` 段で止まるより、まず **pointer/materialization 段まで持続する** とみるのが自然である。したがって `PTR` second-line は target/source だけでなく pointer/materialization にも candidate flavor を通す、と読むのが safest である
- さらに current best bias では、この second-line candidate flavor は final target 決定より **pointer/materialization 段に先に現れる** とみるのが自然である。したがって `PTR` second-line order も `targetSource` reopening -> `pointer/materialization` -> final target の順に寄せるのが safest である
- 現在の code frontier でもこの読みはかなりそのまま反映できていて、first-line の `combatDecision -> postBranchRoute` のあとに **`postBranchTargetSource -> pointerFlavor -> target`** を second-line reopening として並べるのがもっとも自然である。したがって current implementation order も battle-side narrowing とかなり整合している
- したがって current provisional API boundary もかなり明快で、step 6 では **decision layer (`combatDecision`) -> routing core (`postBranchRoute`) -> second-line reopening (`postBranchTargetSource -> pointerFlavor -> target`)** の 3 層を first-line の interface として固定してよい。raw battle work の mirror より、この 3 層 API を先に安定化させるのが safest である
- さらに current best bias では、この second-line reopening の `pointerFlavor` と `target` の間に大きな hidden layer を first-line で置く必要は薄く、まずは **thin final-target step** として扱うのが自然である。したがって current API boundary では `postBranchTargetSource -> pointerFlavor -> target` をそのまま second-line shape として保つのが safest である
- したがって current integrated boundary snapshot では、step 6 の actor-local API は **`combatDecision -> postBranchRoute -> postBranchTargetSource -> pointerFlavor -> target`** の 5 段で整理するのがもっとも自然である。いまは shape を先に固定し、battle-side evidence で各 field の semantics を後から強める段階にある
- この 5 段のうち first-line の `combatDecision` も current best reading ではかなり意味が絞れていて、`accepted` は **local candidate accept-policy bit**、`branch` は **shared strict-fallback branch family**、`branchVariant` は **PTR-only strict-fallback refinement** とみるのが safest である。したがって次の解析は shape の再設計より、これら 3 field の exact semantics を recovered semantics に押し上げることへ集中できる
- このうち `accepted` については current best bias でもう一段整理できていて、`accepted=false` が current consume path denied / strict fallback retained である以上、`accepted=true` も first-line では **current local consume path への admission bit** とみるのが最も自然である。したがって `accepted` は hit/damage 成功ではなく local gate semantics として扱うのが safest である
- さらに current best reading では、この `accepted=true` の直後に読む `branch` / `branchVariant` は fallback 先そのものより、まず **admitted consume path の内側でどの local branch を activate するか** を表すとみるのが自然である。したがって field shape はそのままに、`accepted` の値に応じて `branch` を admitted-path activation / fallback selection のどちらとして読むかを後段に委ねるのが safest である
- このとき `branchVariant` も false 側専用 artifact とみるより、**PTR path の optional refinement field** として true/false の両方で残るとみるのが自然である。したがって current best reading では `branchVariant` は PTR-only field のまま保ちつつ、exact semantics は `accepted` の値に応じて admitted refinement / fallback refinement として読み分けるのが safest である
- さらに current best bias では、この `branchVariant 0/1` 自体は true/false で別の value alphabet を持つより、**shared 0/1 value** を持ち、その reader-side meaning だけが `accepted` の値に応じて変わるとみるのが最も自然である。したがって current frontier では `branchVariant?: 0 | 1` を固定し、consumer semantics を後から強める方針が safest である
- そして current best reading では、この shared `branchVariant 0/1` は first-line の decision layer だけで完結するより、**second-line reopening にも carry される PTR-specific refinement** とみるのが自然である。したがって current frontier では `branchVariant` を narrow decision field であると同時に、`postBranchTargetSource / pointerFlavor / target` の reopening shape を条件づける field として扱うのが safest である
- さらに current best bias では、この `branchVariant` の second-line effect は `postBranchTargetSource` より **`pointerFlavor` に強く出る** とみるのが自然である。したがって PTR-specific second-line reopening の中心は `pointerFlavor` にあり、`postBranchTargetSource` はその前段 marker として読むのが safest である
- そして current best reading では、`postBranchTargetSource` 自体は `branchVariant` に強く依る field というより、**shared post-branch entry marker** とみるのが自然である。したがって second-line の役割分担も `postBranchTargetSource = 弱い入口 marker`、`pointerFlavor = 強い PTR-specific reopening` として読むのが safest である
- さらに current best bias では、final `target` も PTR-specific 差分の中心というより、**`pointerFlavor` の downstream effect** とみるのが自然である。したがって second-line の役割分担は `postBranchTargetSource = 入口 marker`、`pointerFlavor = reopening core`、`target = downstream result` と置くのが safest である
- したがって current field-readiness snapshot では、5 段 shape 自体はかなり stable であり、next step は field を増やすことではなく **`accepted / branch / branchVariant / pointerFlavor / target` の exact semantics を強めること** にかなり集中できる。とくに second-line の中心は `pointerFlavor` で、`target` はその downstream result として扱うのが safest である
- このうち `pointerFlavor` は current best reading でもかなり意味が強く、second-line reopening の **pointer/materialization provenance class** とみるのがもっとも自然である。したがって next analysis の first target を `pointerFlavor` に置き、`target` はその downstream result として読む方針が safest である
- さらに current best bias では、この `pointerFlavor` の exact battle-side meaning も first-line ではかなり絞れていて、`"shared"` は **shared/default pointer provenance**、`"candidate"` は **PTR-specific candidate pointer provenance** とみるのがもっとも自然である。したがって next analysis も shape の再検討ではなく、この provenance semantics の sharpening へかなり集中できる
- そして current best reading では、この `"candidate"` provenance の最有力アンカーは **`D?12..` repeated candidate-entry family** である。したがって `pointerFlavor="candidate"` は generic label ではなく、`D?12..` candidate-entry line の second-line reopening として読むのが safest であり、next analysis もこの family との exact mapping へかなり集中できる
- さらに current best bias では、`branchVariant 0/1` も generic binary switch というより、**`D?12..` entry の code/qualifier 差を first-line へ圧縮した refinement** とみるのが自然である。したがって first-line の `branchVariant` と second-line の `pointerFlavor=\"candidate\"` は、どちらも `D?12..` family 由来の差分を別層で表したものとして読むのが safest である
- この compressed split の current best bias も少し具体化できていて、`branchVariant` は qualifier 差だけより **`0E/0F` family variant 差を primary**、qualifier class 差を **secondary** とする binary refinement とみるのが最も自然である。したがって next analysis も pure code / pure qualifier の二択ではなく、code-led compressed split を first line に置くのが safest である
- さらにこの primary-axis 読みを battle-side wording へ寄せると、`branchVariant` は pure qualifier split ではなく **`0E/0F` special-candidate family difference を主軸に qualifier を副次要因として畳んだ first-line refinement** とみるのが safest である。つまり first-line では `0E/0F` family 差が binary refinement の最強軸で、qualifier `0 / 1 / 2+` はその内側で local gate を調整する secondary modifier として持つのが current best reading である
- この first-line 主軸差を second-line へ延長すると、`pointerFlavor="candidate"` も generic candidate label ではなく **`0E/0F` family 差を主軸にした code-led provenance carry** とみるのが safest である。つまり `branchVariant` が first-line compressed refinement を担い、`pointerFlavor="candidate"` は同じ `D?12..` family 差分を second-line で provenance reopening として見せる、という役割分担が current best reading である
- さらに current best bias では、qualifier `1` blocked ordinal は second-line reopening の主軸までは上がらず、`pointerFlavor="candidate"` の exact semantics の中に **weak blocked-ordinal shadow** として残るとみるのが safest である。つまり second-line の `"candidate"` provenance class は pure code carry でも pure qualifier carry でもなく、**code-led carry with blocked-ordinal shadow** と読むのが current best reading である
- この blocked-ordinal shadow を first-line の `branchVariant` 側へ戻して見ると、qualifier `1` は binary split の主軸ではなく **strict-side polarity を少し強める secondary bias** とみるのが safest である。つまり `branchVariant` は引き続き `0E/0F` 主軸の code-led refinement として読みつつ、blocked ordinal は fast/shortcut 側ではなく strict/non-fast-path 側に shadow を落とす、と持つのが current best reading である
- さらに current best bias では、この strict-side polarity は `branchVariant 0/1` の exact numeric naming より先に fixed できる。つまり今の safest reading は、`branchVariant` の **片側が fast/shortcut-leaning、もう片側が strict/non-fast-path-leaning** であり、qualifier `1` blocked shadow は後者へ寄る、という semantic polarity までである。`0` と `1` の battle-side 名付けは second-line evidence が増えるまで保留するのが安全である
- この strict-side polarity を second-line reopening へ延長すると、`pointerFlavor="candidate"` は generic PTR marker というより **strict-side aligned candidate provenance reopening** とみるのが safest である。つまり first-line の strict/non-fast-path 側 refinement が second-line では candidate provenance として reopening し、`target` はその downstream result として現れる、という alignment を current best reading の fixed point に置くのが安全である
- さらに current best bias では、この strict-side polarity は `accepted=false` 側だけで使い切られるのではなく、`accepted=true` 側でも **向きを保ったまま admitted-path refinement として残る** とみるのが safest である。つまり `accepted` が変えるのは polarity の向きではなく reader-side role であり、same `branchVariant` polarity が false 側では fallback、true 側では activation として読まれる、という形が current best reading である
- 同じく second-line 側でも、`pointerFlavor="candidate"` の strict-side alignment は `accepted=false` 側だけのものではなく、`accepted=true` 側でも **向きを保ったまま admitted-path provenance reopening** として残るとみるのが safest である。つまり `pointerFlavor="candidate"` も true/false で class が変わるのではなく、same alignment を保ったまま fallback-side reopening / admitted-path reopening として role だけが変わる、という形が current best reading である
- さらに current best bias では、この `pointerFlavor` の 2 値もかなり対称に読めて、`"candidate"` が PTR-specific reopening class なら、`"shared"` は **ATK 側を含む default pointer/materialization reopening class** とみるのが最も自然である。したがって `pointerFlavor` は second-line の中心 field としてかなり安定している
- この対称性を cross-acceptance まで延長すると、`pointerFlavor="shared"` も `accepted=false/true` をまたいで **same fast/default alignment** を保つとみるのが safest である。つまり second-line の 2 値は current best reading では、`"candidate" = strict-side aligned provenance reopening`、`"shared" = fast/default aligned provenance reopening` の pair を保ちつつ、`accepted` に応じて fallback-side / admitted-path の role だけが変わる、と読むのが最も自然である
- この前提を採ると、`ATK/PTR` の final `target` 差分も current best reading ではまず **`pointerFlavor` の pair alignment の downstream result** として説明するのが最も自然である。つまり `target` は独立の PTR-specific semantic source というより、`"candidate" = strict-side aligned provenance reopening` と `"shared" = fast/default aligned provenance reopening` という pair の terminal consequence として読むのが safest である。したがって second-line の主観測点は引き続き `pointerFlavor` に置き、`target` はその帰結として読むのが安全である
- この second-line pair 対称を first-line へ戻すと、`branch` も pure strict-fallback family より **fast/default side と strict side を持つ shared branch-family alignment** とみるのが safest である。つまり `accepted=false` 側では same branch pair を fallback selection として読み、`accepted=true` 側では same branch pair を admitted-path activation として読む、という形にかなり自然に揃う
- この first-line pair を second-line reopening へつなぐ中間層として、`postBranchRoute` も opaque continuation id より **branch pair/refinement を pointer pair reopening へ受け渡す alignment transfer core** とみるのが safest である。つまり `combatDecision -> postBranchRoute -> pointerFlavor -> target` は、current best reading では pair alignment を保ったまま downstream へ流れる chain として読むのがもっとも自然である
- さらに current best bias では、`branchVariant` refinement も `postBranchRoute` の中で早々に失われるより、**`pointerFlavor` pair を reopen する直前まで retained compressed refinement** とみるのが safest である。つまり `postBranchRoute` は alignment transfer であると同時に、PTR path では `branchVariant` を second-line reopening まで保持する shared route core と読むのが current best reading である
- そして current best bias では、その retained refinement の主な landing point も `postBranchTargetSource` より **`pointerFlavor`** にあるとみるのが safest である。つまり route core は `branchVariant` を second-line に carry するが、その strongest effect は shared entry marker ではなく pointer-side reopening core に現れ、`target` はその downstream terminal として読むのが current best reading である
- したがって current readiness snapshot では、5-layer shape / layer order / field split はかなり code-ready とみてよく、still-provisional な論点は主に **field values の exact named semantics** と **`postBranchRoute` の exact battle-side anchoring** に集中している。つまり next analysis は field を増やすことではなく、`pointerFlavor / branchVariant / branch / postBranchRoute` の exact meaning を sharpen することへかなり集中できる
- さらに current best bias では、この `postBranchRoute` の exact battle-side anchoring も wide staging より **`41A4-41EC` 近辺の actor-local opener から second-line reopening へ移る narrow route-core belt** に置くのが safest である。つまり unresolved 点は route-core の存在そのものではなく、`41A4-41EC` 帯のどこまでを exact anchor と数えるかへかなり集約されている
- この narrow belt も current best reading では一様ではなく、**`41D9-41E5` を route-core nucleus、`41C4-41D8` を pre-filter halo、`41E6-41EC` を consume halo** とみるのが safest である。つまり `postBranchRoute` の exact anchor をさらに絞るなら、`41A4-41EC` 全体より `41D9-41E5` を first-line の本命 nucleus に置くのが自然である
- さらに current best bias では、`41D9-41E5` nucleus 自体も一様ではなく、exact route-core effect は先頭より **`41E6` 直前の後半 handoff edge (`41E3-41E5` 近辺)** に寄るとみるのが safest である。つまり nucleus の前半は route material gathering、後半は retained refinement を second-line reopening / consume halo へ渡す局所境界として読むのが current best reading である
- この handoff 読みを second-line 側へ延長すると、`pointerFlavor` reopening の最初の実効点は `41E3-41E5` の内部ではなく **`41E6+` の reopening/consume halo 入口** に置くのが safest である。つまり `41E3-41E5` は route-core handoff に留まり、`41E6-41EC` が first effective reopening/consume zone として立ち上がる、という時間順が current best reading である
- したがって `postBranchRoute` と second-line reopening の current safest boundary wording は、**`41E3-41E5` = retained refinement handoff edge、`41E6+` = first effective reopening / consume onset** である。`pointerFlavor` は route-core の内部 field ではなく、この handoff を受けたあとに初めて meaning を持つ reopening field と読むのが safest である
- さらに finer wording では、`41E6+` を second-line reopening / consume halo の structural onset、**`41E7-41E9` をその内部で最初に local decision が visible になる effective slot** と分けて持つのが safest である。つまり `41E6` は halo entry、`41E7-41E9` は first effective local decision slot、`41EB-41EC` は deterministic consume/writeback とみるのが current best reading である
- この `41E7-41E9` effective slot の primary emphasis は、current frontier では consume gate そのものより **`pointerFlavor` reopening が最初に visible になる slot** に置くのが safest である。consume gate meaning はその immediate consequence として読むのが current best reading である
- さらに current best bias では、`41E7-41E9` で visible になるのは generic reopening より **`pointerFlavor` の pair alignment (`shared` / `candidate`)** である。つまりこの帯は first effective local decision slot であると同時に、second-line の provenance side が最初に見える slot と読むのが safest である
- この pair visibility は `postBranchTargetSource` を強い landing pointとして経由するより、**weak/shared marker を素通り気味にして実質 `pointerFlavor` に直接 landing する** とみるのが safest である。したがって second-line で最初に本当に差分化する主戦場は `postBranchTargetSource` ではなく `pointerFlavor` にある
- さらに current best reading では、`pointerFlavor` の `shared/candidate` pair difference は final `target` を直接 encode するより、**target-determination provenance path を downstream で分岐させる path discriminator** とみるのが safest である。つまり `target` は `pointerFlavor` pair の terminal consequence として扱うのが current best reading である
- この path-discriminator 読みを exact naming へ少し寄せるなら、`pointerFlavor` の 2 値は **`"shared"` = shared/default target-provenance path、`"candidate"` = candidate-entry target-provenance path** と書くのが safest である。つまり `"candidate"` は generic PTR marker ではなく `D?12..` candidate-entry family に anchored した provenance class と読むのが current best reading である
- 同じ粒度で first-line を exact naming へ寄せるなら、`branch` は **local-resolution mode pair**、`branchVariant` は **PTR-only candidate-family refinement bit**、`postBranchRoute` は **local-resolution mode から target-provenance reopening へ渡す alignment-transfer core** と書くのが safest である。これで first-line は mode/refinement、second-line は provenance/path/result という role split がかなり明確になる
- `branchVariant 0/1` の numeric polarity については、current frontier では **shared/default-leaning side と candidate-aware/strict-leaning side** という semantic polarity までは固定できるが、`0` と `1` の exact binding は still provisional として保留するのが safest である
- したがって current safest implementation policy は、**raw numeric fields はそのまま保持し、debug/docs では side semantics を併記する** ことである。特に `branchVariant` は `0|1` を premature に named side へ固定せず、shared/default-leaning side と candidate-aware/strict-leaning side という wording を先に強めるのが safest である
- battle-side exact anchoring をさらに絞ると、current best bridge は **`41E3-41E5` = `postBranchRoute` の retained-refinement handoff edge、`41E7-41E9` = `pointerFlavor` pair の first effective visibility slot** と置くのが safest である。`41E6` はその間の reopening/consume halo entry と読むのが current best wording である
- この `41E7-41E9` effective slot の内部 role split は、current frontier では **`pointerFlavor` pair visibility が first-line で立ち上がり、その immediate consequence として local decision / gate が読める overlap** とみるのが safest である。つまり主語は visibility、gate は secondary consequence という主従関係で持つのが current best reading である
- 時間順を 1 本でまとめる current safest wording は、**`41E3-41E5` = retained refinement handoff edge、`41E6` = reopening/consume halo entry、`41E7-41E9` = first effective `pointerFlavor` pair visibility slot、`41EB-41EC` = deterministic consume/writeback** である。つまり transfer core -> halo entry -> effective reopening -> deterministic effect という 4 段 chain として読むのが current best reading である
- この temporal chain を前提にすると、5-layer provisional API は **shape / order / layer-role の面ではかなり recovered semantics に近づいている** とみるのが safest である。残る主課題は decomposition の再設計ではなく、`branch` / `branchVariant` / `pointerFlavor` の exact naming と `41E3-41E5 -> 41E6 -> 41E7-41E9` の finer timing wording をさらに sharpen することである
- `branch` の exact naming を current frontier で一段 battle-native に寄せるなら、これは **actor-local resolution lane pair** と読むのが safest である。つまり side names は `default resolution lane` と `candidate-aware resolution lane` に寄せるのが current best wording である
- この lane wording に揃えるなら、`branchVariant` は **PTR-only candidate-family lane refinement bit** と読むのが safest である。side semantics も `shared/default-leaning lane refinement` と `candidate-aware/strict-leaning lane refinement` に寄せるのが current best wording である
- 同じ語彙で `postBranchRoute` を sharpen するなら、これは **lane-transfer core** と読むのが safest である。つまり actor-local resolution lane / lane refinement を second-line の target-provenance reopening へ渡す transfer core と書くのが current best wording である
- `branchVariant 0/1` については、current frontier では **side semantics はかなり回収できているが numeric binding はまだ回収し切っていない** とみるのが safest である。したがって raw `0|1` は保持しつつ、shared/default-leaning side と candidate-aware/strict-leaning side という semantic polarity を先に使う implementation policy が current best reading に最も合う
- いっぽうで `branchVariant` raw value と second-line の `pointerFlavor="shared"/"candidate"` の対応自体は、current frontier では **かなり strong な side-level correspondence** とみるのが safest である。つまり raw value の片側が `"shared"` に、もう片側が `"candidate"` に対応すると読むのはかなり自然だが、どちらが `0` でどちらが `1` かの numeric binding だけが still provisional である
- したがって current safest trace/debug wording は、**raw `0|1` をそのまま表示しつつ、side semantics と `pointerFlavor` との same-side correspondence を別に書く** 形である。numeric value を second-line side name へ直結させる wording は、exact binding が取れるまで避けるのが safest である
- あわせて current safest debug field order は **`branch -> branchVariant -> pointerFlavor`** である。つまり first-line の lane pair と refinement を先に見せ、その carried result として second-line の provenance reopening side を後に見せるのが current best reading に最も合う
- さらに `postBranchRoute` を含めるなら、current safest insertion point は **`branch -> branchVariant -> postBranchRoute -> pointerFlavor`** である。つまり first-line lane / refinement の直後に lane-transfer core を挟み、その landing point として second-line provenance reopening を見せるのが current best reading に最も合う
- この order の最後に `target` を置くのが safest なのは、current best reading では `target` が **`pointerFlavor` pair の downstream terminal result** だからである。したがって debug / trace でも、path discriminator を先に見せ、その terminal consequence として `target` を最後に置くのが current best policy である
- current source 側の debug policy もこの reading に合わせて、**`branch -> branchVariant -> postBranchRoute -> pointerFlavor -> target`** の順に見せ、`postBranchTargetSource` は `marker` / `m` として弱く扱い、`target` は `target terminal` / `tgt` として downstream consequence であることを明示するのが safest である
- current canonical wording の入口としては、first-line naming / transfer core / second-line naming / temporal chain / debug policy をそれぞれ index 化して参照するのが safest である。実用上の one-line model は **`branch` = actor-local resolution lane pair、`branchVariant` = PTR-only candidate-family lane refinement bit、`postBranchRoute` = lane-transfer core、`pointerFlavor` = target-provenance path pair、`target` = downstream terminal result** である
- この canonical model の上でなお decisive に残る frontier は、**`branchVariant 0/1` の numeric binding、`41E3-41E5 -> 41E6 -> 41E7-41E9` の finer timing wording、`postBranchRoute` の exact micro-boundary** の 3 本である。したがって今後の解析は shape や API の再設計ではなく、この 3 点の semantic sharpening に集中するのが safest である
- このうち `branchVariant 0/1` の numeric binding を固定してよい条件は、current frontier では **raw `0/1` と `pointerFlavor` side の one-way に近い direct correspondence**、または **`0E/0F` family split と raw `0/1` split を同一局所 cluster で anchor する stronger local evidence** のどちらかが得られることである。そこに至るまでは raw numeric を保持し、side semantics を使い続ける implementation rule が safest である
- `41E7-41E9` の finer timing wording については、current frontier ではこれを **visibility-led micro-sequence** とみるのが safest である。つまり first effective `pointerFlavor` pair visibility が立ち上がり、その immediate consequence として local gate / decision が同じ slot 内で続く、と読むのが current best wording である
- `41E3-41E5` の exact micro-boundary wording については、current frontier では single exact point に固定するより **route-core terminal band / retained refinement handoff edge** と呼ぶのが safest である。つまり `41D9-41E2` を material gathering、`41E3-41E5` を terminal band、`41E6` を halo entry と分けるのが current best wording である
- 以上を総合すると、5-layer provisional API は current frontier では **shape だけでなく naming / timing / debug policy まで含めてかなり強い provisional semantic closure** に達しているとみるのが safest である。残る主課題は interface 発見ではなく、`branchVariant 0/1` の numeric binding と local cluster wording の finer sharpening である
- この closure snapshot を implementation policy に戻すと、**API shape / layer role / semantic debug policy は GO、`branchVariant 0/1` の numeric binding と instruction-level anchoring は NO-GO** と切り分けるのが safest である。つまり今後は shape を実装しつつ、fine-grain evidence が揃うまで binding と opcode-level assumption は保留するのが current best policy である
- この GO / NO-GO split を実作業へ戻すなら、直近の implementation backlog は **core contract stabilization / debug ordering maintenance / frontend bridge usage / shape-oriented selfcheck 強化** である。逆に **numeric binding / opcode-level split / fine timing dependent behavior** は evidence 待ち backlog として保留するのが safest である
- 現在の source 側では、この方針が `debugTrace` / UI wording / selfcheck にも反映されており、**`combat hook -> post-branch marker + pointer -> target terminal`** の順を code-ready debug contract として扱うのが safest である。これにより raw numeric binding を固定しないまま、side semantics と layer order を十分強く保証できる
- したがって `to 5` の current progress を 1 本でまとめると、いまは wide route search の段階ではなく **`41E3-41E5` handoff edge と `41E6+` reopening/consume halo の境界を sharpen する段階** に入っている。shape / layer order / field split はかなり code-ready で、残る主課題は `postBranchRoute` の exact micro-boundary と `branch / branchVariant / pointerFlavor` の exact named semantics である
- `branchVariant` binding の next strategy も current frontier ではかなり明確で、必要なのは semantic polarity の再構成ではなく **raw `0/1` をどちら側 semantic に貼りつけてよいかを lock する local evidence acquisition** である。binding lock を許す条件は、(1) raw `0/1` と `pointerFlavor` side pair の one-way に近い direct correspondence、または (2) raw `0/1` split と `0E/0F` family split の同一局所 cluster anchoring のどちらかであり、そこに届くまでは **raw numeric は保持し、side semantics だけを強く使う** のが safest である
- `41E7-41E9` の effective slot を current frontier でさらに限定して言うなら、ここで strongest に visible になるのは raw numeric decoding ではなく **`branchVariant` と `pointerFlavor` の same-side correspondence** である。つまりこの slot は `0 == shared` を lock する場ではなく、**PTR-only lane refinement と target-provenance pair reopening が first effective に同じ側へ揃って見える slot** と読むのが safest である
- したがって numeric binding を本当に lock する主 cluster は `41E7-41E9` 単独ではなく、**`41E3-41E9` の handoff-to-visibility chain** とみるのが safest である。current best wording では `41E7-41E9` は first effective same-side visibility slot、いっぽう `41E3-41E9` は **minimal branchVariant binding candidate cluster** であり、binding lock は retained refinement carry と visible pair landing をこの chain 全体で見て初めて許される
- この `41E3-41E9` cluster をさらに sharpen するなら、numeric binding lock の first-line 条件は **single-side carry** がどこまで一方向に追えるかである。current frontier では、retained refinement は `41E3-41E5` handoff edge から `41E6` halo entry を跨いで `41E7-41E9` same-side visibility slot へかなり自然に carry される bias を持つが、raw `0/1` 自体を named side へ貼りつける stronger local evidence はまだ不足している。したがって safest wording は、`41E3-41E9` を **single-side carry candidate cluster** と呼び、binding lock はまだ保留する、である
- この single-side carry candidate cluster の next decisive point は hidden remap の有無であり、current frontier では **large hidden remap を仮定するより remap-free carry を仮定するほうが自然** とみるのが safest である。つまり `41E3-41E5` handoff edge、`41E6` halo entry、`41E7-41E9` same-side visibility slot を並べると、current best interpretation は **remap-free carry bias** を持つ chain だが、raw `0/1` binding を lock する direct proof にはまだ至っていない
- この chain の中で raw `0/1` binding を strongest に運ぶ sub-span も current frontier ではかなり絞れており、safest ranking は **`41E3-41E5` > `41E6` > `41E7-41E9`** である。つまり `41E3-41E5` は retained refinement の last dense point として binding evidence の本命帯、`41E6` は boundary witness、`41E7-41E9` は same-side visibility の主観測点、と役割分担を切るのが current best wording である
- ただし `41E3-41E5` も current frontier では **strongest binding-candidate band** までは言えても、raw `0/1` が named side へ局所的に貼りつく **direct local anchor confirmed band** とまではまだ言わないのが safest である。残っている gap は、この帯の中で raw `0/1` と side semantics、あるいは `0E/0F` family split が stronger に同一局所対応する evidence がまだ不足している点にある
- この direct local anchor gap を埋める本命軸については、current frontier では **`0E/0F` special-candidate family split** に strongest に寄せるのが safest である。つまり `branchVariant` は pure qualifier split より code-led split を主軸にした refinement と読めているため、`41E3-41E5` の中で raw `0/1` binding を lock する最短線も side-semantic direct anchor より **code-family split anchor** にある、とみるのが current best wording である
- さらに current best reading では、`41E3-41E5` が strongest に carry している semantic payload 自体も **`0E/0F` code-family split** とみるのが safest である。つまりこの帯は handoff edge であるだけでなく **code-split carry strongest band** として読めるが、raw `0/1` と `0E/0F` side の direct local binding までは still unconfirmed である
- したがって current frontier に残る gap はかなり narrow であり、もはや broad semantic uncertainty ではなく **raw `0/1` と `0E/0F` code-family side の stronger local correspondence gap** と呼ぶのが safest である。side semantics、same-side carry、same-side visibility、remap-free carry bias、strongest band、strongest payload までは回収済みなので、残っているのは one-step local glue だけである
- この one-step local glue が未回収でも、implementation scope はかなり広く GO にできる。current safest policy では、`branchVariant?: 0 | 1` の **field shape / optionality / transport / side-semantic wording / same-side correspondence / ordering / selfcheck** は GO であり、NO-GO は **raw `0/1` に exact side name を焼き込むこと** と **raw numeric を semantic branch key として直接使うこと** にかなり収束している
- source 側の current contract もこの方針に揃えてあり、いまは **raw `branchVariant?: 0 | 1` は保持しつつ、`branchVariantMeaning = candidate_family_lane_refinement_bit` と `branchVariantBindingStatus = deferred_numeric_binding` を返す** 形が safest である。つまり source は same-side carry semantics を強く保ちながら、exact numeric binding だけを明示的に deferred にしている
- したがって `branchVariant` の current code-ready contract もかなり明確で、**field shape / field-level meaning / binding-deferred status / same-side carry relation** の 4 点はそのまま implementation に使ってよい。未回収なのは raw `0/1` と exact side name の glue だけであり、これは future semantic sharpening item として deferred にできる
- 同じ粒度で見ると、`branch` / `postBranchRoute` / `pointerFlavor` も current frontier で十分 code-ready である。つまり semantic core は **`branch = actor-local resolution lane pair` / `postBranchRoute = lane-transfer core` / `pointerFlavor = target-provenance path pair`** として implementation に使え、残る未確定は exact battle-native naming や instruction-level anchoring の finer sharpening にかなり収束している
- さらに current snapshot では semantic core だけでなく、**frontend preview surface / debug ordering / selfcheck policy** まで含めて implementation-ready とみてよい。つまり current safest operational contract は、semantic core を UI で観測でき、`combat hook -> post-branch marker + pointer -> target terminal` の debug policy を selfcheck で固定しつつ、raw numeric binding と opcode-level anchoring だけを deferred にして進める形である
- この状態での next source work は semantic discovery より **contract hardening** に寄せるのが safest である。つまり current backlog は、5 field の surface consistency、deferred-binding guardrails、selfcheck semantic contract expansion、frontend debug clarity、preview matrix coverage を強めることが中心であり、raw numeric binding や opcode-level semantics の焼き込みは explicit NO-GO に置くのが current best policy である
- current operational contract を再圧縮すると、semantic core / deferred-binding policy / frontend preview / debug ordering / selfcheck はすでに implementation-ready であり、未確定は raw `branchVariant 0|1` の exact side naming と opcode-level anchoring にかなり集中している。したがって今後は shape-preserving implementation を進めつつ、binding と instruction-level semantics だけを explicit deferred item として扱うのが safest である
- この snapshot を次の implementation step に戻すなら、preview matrix expansion も **semantic redesign** ではなく **coverage widening under deferred-binding policy** として進めるのが safest である。優先すべきなのは ability class diversity、target-mode diversity、PTR retention cases であり、matrix 拡張中も raw `branchVariant 0|1` へ exact side naming を与えないことが current best policy である
- 現在の preview matrix もこの方針で広がっており、**`ATK / ATKX / ATKS / DEF / PTR / ABL...`** を最小有効セットとして、shared/default lane、candidate path、explicit target、slot fallback、ability path を同じ 5-layer contract で観測できる。つまり matrix は current frontier では exact binding probe ではなく **deferred-binding policy 下の coverage widening surface** と読むのが safest である
- さらに ability path にも explicit-target probe が入り、current 最小有効セットは **`ATK / ATKX / ATKS / DEF / PTR / ABLX0 / ABL...`** とみるのが safest である。これにより preview matrix は attack 系だけでなく ability 系でも target-mode diversity を含む operational coverage surface として機能している
- さらに ability family にも slot-fallback counterpart が入り、current 最小有効セットは **`ATK / ATKX / ATKS / DEF / PTR / ABLX0 / ABLS0 / ABL...`** とみるのが safest である。これにより preview matrix は attack family と ability family の両方で target-mode symmetry を持つ cross-family coverage surface として読める
- この状態をさらに圧縮すると、current preview matrix は **cross-family coverage surface** と呼ぶのが safest である。つまり `ATK / ATKX / ATKS` が shared/default attack family、`ABLX0 / ABLS0 / ABL...` が ability family、`PTR` が candidate family、`DEF` が distinct local lane family を担い、これらを same 5-layer contract のもとで横並びに比較できる
- deferred-binding 実装方針を最短で書くなら、**raw `branchVariant?: 0 | 1` は保持し、semantic metadata (`candidate_family_lane_refinement_bit`, `deferred_numeric_binding`, `same_side_pointer_correspondence`) を併記し、exact numeric interpretation は遅延する** のが safest である
- current matrix/selfcheck pair を operational readiness の観点でみると、**command-class diversity / target-mode diversity / candidate-vs-shared path diversity** の 3 方向を deferred-binding policy のままかなり安定して観測できる。したがって preview matrix は now “just debug UI” ではなく **regression-guarded operational coverage surface** として扱ってよい
- この状態での GO source work を practical checklist に落とすと、主眼は **5-layer field order の維持 / deferred-binding wording の維持 / semantic field 優先 / coverage widening / selfcheck alignment** にある。逆に raw `branchVariant 0|1` の exact naming や numeric-led branching は current frontier では explicit NO-GO に置くのが safest である
- 以上を source hardening progress としてまとめると、current source は **deferred-binding policy / semantic debug ordering / selfcheck contract / preview matrix coverage** の 4 本でかなり strong であり、remaining weakness は raw `branchVariant 0|1` の exact side naming と opcode-level anchoring にかなり押し出されている。つまり current source work は semantic widening ではなく contract-consistency maintenance と読むのが safest である
- preview matrix が expanded した current frontier では、front 側の参照 policy も **growth-safe** である必要がある。したがって canonical probe (`ATK`, `PTR` など) を fixed index ではなく stable label で引くのが safest であり、これにより coverage widening を続けても canonical debug display が壊れない
- 以上を alignment の観点でまとめると、current frontier では **core semantic contract / frontend preview matrix / debug wording / selfcheck** の 4 層がかなり強く揃っている。growth-safe label lookup まで入ったことで、この aligned surface の外に残っている不確定要素は raw numeric binding と opcode-level anchoring にかなり集中している
- さらに expanded matrix の current UI policy を practical に言うなら、preview readability も contract の一部である。つまり current safest policy は **stable label lookup + family prefix + fixed semantic field order** を併用して、cross-family coverage surface の scannability を保つことである
- ここまでを最短で圧縮すると、current operational surface は **semantic core + deferred binding + cross-family preview coverage + fixed debug order + selfcheck regression guard** の組として扱うのが safest である。すなわち implementation は shape/ordering/coverage を進めてよく、exact numeric binding と opcode-level anchoring だけを deferred item として保てばよい
- finish/handoff 観点でさらに圧縮すると、current state は **shape-preserving implementation and coverage growth are GO, exact branchVariant binding and opcode-native semantics are intentionally deferred** と書くのが safest である。再開点もかなり明確で、analysis 側は `41E3-41E5 / 41E3-41E9 / 41E7-41E9`、implementation 側は preview matrix expansion / label-based canonical probes / selfcheck reinforcement に集中してよい
